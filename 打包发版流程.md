# 打包发版全流程（生产部署手册）

> 适用系统：REIT-AI 申报系统
> 生产地址：http://193.112.194.61
> 最后验证时间：2026-08-07（全流程实测通过）

---

## 一、环境基本事实（务必先读）

| 项 | 值 |
|---|---|
| 服务器 | 腾讯云轻量（广州），Ubuntu 24.04，2 核 2G |
| 登录用户 | `ubuntu`（sudo 需密码，用 `echo 密码 \| sudo -S` 方式） |
| **运行中的 compose 目录（真实生效）** | **`/home/ubuntu/reit-app/`**（2026-08-14 实测：容器 working_dir 在此，数据卷相对路径 `./data/workspace` 解析到真实数据；发版 rsync 必须打这里） |
| 官方代码目录（同步保持不落后） | `/home/ubuntu/REIT-AI-System/`（其 compose 数据卷为绝对路径指向同一数据，也随发版同步） |
| **compose 项目名** | **`reit-app`**（容器：`reit-app-app-1`、`reit-app-nginx-1`） |
| 数据目录（bind 挂载） | `/home/ubuntu/reit-app/data/workspace/` → 容器内 `/app/workspace` |
| 数据库 | `data/workspace/app/backend/database/reits.db` |
| 服务结构 | nginx（80 端口）→ 反代 app 容器（内部 8000，uvicorn + 静态前端） |
| 健康检查 | `curl http://127.0.0.1/api/health`（走 nginx） |
| 配置文件 | `/home/ubuntu/reit-app/.env` 与 `/home/ubuntu/REIT-AI-System/.env`（API Key 等，**不可丢失**） |

⚠️ **四条铁律**：
1. 重建必须带项目名：`docker compose -p reit-app up -d --build`。不带 `-p` 会另起一套 `reit-ai-system-*` 容器，nginx 抢 80 端口报错。
2. `rsync --delete` 同步时必须 `--exclude=.env`（本地打包不含 .env，不 exclude 会把服务器的 .env 删掉，服务直接起不来）。
3. 打包必须排除 `._*`（macOS AppleDouble 垃圾文件，混进镜像会导致启动时 JSON 加载 `UnicodeDecodeError` 崩溃，2026-08-07 实测踩坑）。
4. **同步必须 `--exclude=docker-compose.yml`**：两份服务器 compose 的数据卷都指向真实数据 `/home/ubuntu/reit-app/data/workspace`（reit-app 目录用相对路径 `./data/workspace`、官方目录用绝对路径），本地是相对路径且解析位置不同。一旦被本地版覆盖，容器会挂空数据目录——线上项目“全丢”、admin 密码失效（2026-08-10 实测踩坑）。
5. **同步目标以容器实际 working_dir 为准**：部署前先 `docker inspect reit-app-app-1 --format '{{index .Config.Labels "com.docker.compose.project.working_dir"}}'` 确认（当前为 `/home/ubuntu/reit-app`），只同步官方目录而漏了真实目录，重建后线上仍是旧代码（2026-08-14 实测确认）。

---

## 二、部署前检查：确认没有正在生成的任务

重启容器会打断 AI 生成任务，**必须先查**。任务运行中会有到外部模型服务的活跃连接：

```bash
# 容器内 ESTABLISHED 连接数为 0 才可安全部署
docker exec reit-app-app-1 sh -c "awk 'NR>1 && \$4==\"01\"' /proc/net/tcp /proc/net/tcp6 | wc -l"
```

- 结果为 `0` → 可以部署。
- 结果 `> 0` → 每 60 秒轮询一次，等归零再部署（生成任务通常数分钟内结束）。

---

## 三、标准发版流程（全量：后端/前端/配置有改动）

### 1. 本地提交

```bash
cd REIT-AI-System/REIT-AI-System
git add -A
git commit -m "feat/fix: 本次改动说明"
```

### 2. 打包（注意 COPYFILE_DISABLE 与排除项）

```bash
cd REIT-AI-System/REIT-AI-System
COPYFILE_DISABLE=1 tar \
  --exclude='.git' \
  --exclude='workspace' \
  --exclude='__pycache__' \
  --exclude='*.log' \
  --exclude='.env' \
  --exclude='.deps_installed' \
  --exclude='._*' \
  --exclude='docker-compose.yml' \
  -czf /tmp/reit_deploy.tar.gz .
```

### 3. 上传 + 同步 + 重建（paramiko，本机无 pty 不能用 expect/交互 ssh）

```python
import paramiko, subprocess

SERVER, USER, PWD = '193.112.194.61', 'ubuntu', '<服务器ubuntu密码>'

cli = paramiko.SSHClient()
cli.set_missing_host_key_policy(paramiko.AutoAddPolicy())
cli.connect(SERVER, username=USER, password=PWD, timeout=30)

# 上传压缩包
sftp = cli.open_sftp()
sftp.put('/tmp/reit_deploy.tar.gz', '/tmp/reit_deploy.tar.gz')
sftp.close()

cmd = (
    'rm -rf /tmp/reit-deploy && mkdir -p /tmp/reit-deploy && '
    'tar -xzf /tmp/reit_deploy.tar.gz -C /tmp/reit-deploy && '
    f'echo {PWD} | sudo -S rsync -a --delete '
    '--exclude=data --exclude=workspace --exclude=.env --exclude=docker-compose.yml '      # 数据/密钥/服务器专用compose 不覆盖
    '/tmp/reit-deploy/ /home/ubuntu/reit-app/ && '                                          # 真实运行目录（必打）
    f'echo {PWD} | sudo -S rsync -a --delete '
    '--exclude=data --exclude=workspace --exclude=.env --exclude=docker-compose.yml '
    '/tmp/reit-deploy/ /home/ubuntu/REIT-AI-System/ && '                                    # 官方目录同步
    f'cd /home/ubuntu/reit-app && echo {PWD} | sudo -S '
    'docker compose -p reit-app up -d --build'
)
stdin, stdout, stderr = cli.exec_command(cmd, timeout=500)
print(stdout.read().decode()[-500:])
cli.close()
```

### 4. 等待并验证健康（容器重建后有 20~60 秒启动期，期间 502 属正常）

```bash
# 每 10 秒查一次，最多等 2 分钟
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1/api/health   # 期望 200
```

超过 2 分钟仍 502，看日志定位：

```bash
echo <密码> | sudo -S docker logs reit-app-app-1 --tail 40
```

### 5. 验证改动生效（示例：确认前端新代码已上线）

```bash
curl -s http://localhost/js/app.js | grep -c '<新代码中的关键字>'
```

---

## 四、快速通道（只改前端静态文件，无需重建镜像）

前端由 app 容器内 uvicorn 直接从磁盘读静态文件，`docker cp` 即时生效：

```bash
# 1. 同步到宿主机源码目录（保证下次重建不回退）
scp frontend/js/app.js ubuntu@193.112.194.61:/home/ubuntu/REIT-AI-System/frontend/js/app.js

# 2. 注入运行中的容器（paramiko 方式）
echo <密码> | sudo -S docker cp \
  /home/ubuntu/REIT-AI-System/frontend/js/app.js \
  reit-app-app-1:/app/frontend/js/app.js

# 3. 验证（nginx 对静态资源可能有缓存，必要时带 ?v=1 或强刷）
curl -s http://localhost/js/app.js | grep -c '<关键字>'
```

适用：`frontend/js/*.js`、`frontend/css/*.css`、`frontend/index.html`。
不适用：后端 Python 代码、requirements 变更、nginx 配置（需走标准流程）。

---

## 五、回滚

发版前在服务器备份当前代码目录：

```bash
# 备份真实运行目录（命名规则 reit-app.bak.MMDDHHMM，含数据卷）
echo <密码> | sudo -S cp -a /home/ubuntu/reit-app /home/ubuntu/reit-app.bak.$(date +%m%d%H%M)
```

出问题时：

```bash
echo <密码> | sudo -S rsync -a --delete --exclude=data --exclude=.env --exclude=docker-compose.yml \
  /home/ubuntu/reit-app.bak.XXXX/ /home/ubuntu/reit-app/
echo <密码> | sudo -S bash -c 'cd /home/ubuntu/reit-app && docker compose -p reit-app up -d --build'
```

数据在 `data/` 目录（rsync 已 exclude），回滚不影响用户数据。

---

## 六、常见故障排查（均已实测验证）

| 现象 | 根因 | 处理 |
|---|---|---|
| `env file .env not found` | rsync --delete 把服务器 .env 删了 | 重新上传 .env 到 `/home/ubuntu/REIT-AI-System/.env` 再重建 |
| `Bind for 0.0.0.0:80 failed: port is already allocated` | 误起了重复 compose 项目（没带 `-p reit-app`） | `docker compose -p <误起的项目名> down`，再用 `-p reit-app` 重建 |
| 容器反复 Restarting，日志 `UnicodeDecodeError ... invalid start byte` | `._xxx.json` 等 macOS 垃圾文件被打进镜像 | 删除该文件，tar 加 `--exclude='._*'`，重建 |
| 部署后持续 502 | 容器健康检查启动期（start_period 20s + retries） | 等 60 秒再测；仍 502 看 `docker logs` |
| `No such container: reit-ai-server` | 容器名已变更为 `reit-app-app-1` | 用 `docker ps` 确认现名 |
| 容器内 `python -c "from database.db ..."` 报 ModuleNotFoundError | 新版代码用 `backend.` 包路径，workdir 为 /app | 调试脚本用 `docker cp` 传文件进容器再执行，避免引号转义地狱 |

---

## 七、日常运维速查

```bash
# 查看容器状态
docker ps --format "{{.Names}} | {{.Status}}"

# 查看实时日志
echo <密码> | sudo -S docker logs -f --tail 50 reit-app-app-1

# 重启服务（不打断数据，但会打断生成任务，先做第二节检查）
echo <密码> | sudo -S docker restart reit-app-app-1

# 进容器排查
docker exec -it reit-app-app-1 sh

# 数据库探查（只读）
docker cp probe.py reit-app-app-1:/tmp/ && docker exec reit-app-app-1 python /tmp/probe.py
```

---

## 八、发版检查清单

- [ ] 本地改动已 commit
- [ ] 已确认无进行中的生成任务（第二节）
- [ ] 已用 docker inspect 确认容器 working_dir，同步目标包含真实运行目录
- [ ] tar 排除项齐全（尤其 `._*`、`.env`、`docker-compose.yml`）
- [ ] rsync 带 `--exclude=data --exclude=workspace --exclude=.env --exclude=docker-compose.yml`
- [ ] 重建命令带 `-p reit-app`
- [ ] health 返回 200
- [ ] 关键改动在线上文件/接口中可验证到
- [ ] 用户侧强刷页面验收
