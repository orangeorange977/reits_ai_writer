# "📎 依据"链接打不开问题 —— 完整修复记录

> 日期：2026-08-07　|　范围：章节正文里"📎 依据"点击跳转原文的功能
> 目的：记录所有踩过的坑与最终匹配规则，避免以后类似问题再次出现。

---

## 一、背景

章节由 AI 生成时，每句/每表会带一条"📎 依据"标注，用户点击后应打开对应的申报材料原文并高亮摘录句。
依据文本由 AI 自由生成，**格式五花八门**，而文件在磁盘上的真实路径又受上传截断、目录层级等影响，
两边对不上就会出现"点不开"。本轮共暴露并修复了 **7 类问题**。

---

## 二、问题清单（现象 → 根因 → 修复）

### 问题 1：多条依据〈1〉〈2〉粘连，第一条也点不开
- **现象**：`📎 依据：〈1〉申报材料：…docx 〈原文摘录〉… 〈2〉同上文件 〈原文摘录〉…` 整条点不开。
- **根因**：旧解析只按"；"拆分，`〈2〉…` 整段被搅进第一条的文件路径，路径变成无效字符串。
- **修复**：`openSrcLink` 先按 `〈数字〉` 拆出每条依据，再按"；"拆（兼容旧格式），逐条解析。

### 问题 2：不认识"同上文件"
- **现象**：依据写"同上文件"时点击无反应/报无法定位。
- **根因**：旧代码完全没有该分支。
- **修复**：新增 `_lastSrcMaterialPath` 全局变量——每次成功打开一条"申报材料"依据就记下路径；
  遇到"同上文件"时复用它打开同一文件（并高亮各自的摘录）。

### 问题 3：摘录截取错位
- **现象**：打开原文后高亮不到摘录句。
- **根因**：旧代码把 `〈原文摘录〉` 这个**标记词本身**当成了摘录内容，真正的摘录被混进路径。
- **修复**：以 `〈原文摘录〉` 为界切分——前面是文件路径，后面才是要高亮的摘录内容。

### 问题 4：空项目生成的章节，依据全部点不开
- **现象**："材料加载失败，文件不存在或已被删除" / "未在当前项目材料库中找到该文件"。
- **根因**：服务器上"万国数据"项目材料库 **0 个文件**却生成过章节，依据指向的文件根本不存在。
  **诊断方式**：写脚本把所有章节 src 逐一比对材料库（本项目测试case 55 条依据全部命中、0 缺失，
  从而排除"文件真丢了"，锁定是空项目）。
- **修复**：报错文案升级——说清"哪个项目、缺哪个文件、怎么办"（补传指引），不再显示原始技术报错。
- **教训**：**材料为空的项目不该让用户以为能溯源**；以后可考虑生成前提示"未上传材料"。

### 问题 5：AI 把文件名简写，与磁盘官方全称对不上（本轮主犯）
- **现象**：`材料库里没有《9-1 法律意见书.docx》`，但文件明明已上传。
- **根因**：依据里是 AI 简写 `9-1 法律意见书.docx`，磁盘上是官方全称
  `9-1 律师事务所就项目权属、资产范围和项目合法合规性出具的法律意见书.docx`，
  两个字符串互不包含，原模糊匹配（精确→互含→主干）全部落空。
- **修复**：`_findMaterialPath` 增加第 4 级匹配——**编号前缀 + 核心词**：
  `9-1 法律意见书` → 找磁盘上以 `9-1` 开头且文件名含 `法律意见书` 的文件；
  同编号有 docx/pdf 双版本时，优先依据里指明的扩展名。

### 问题 6：AI 把依据路径的目录层级写错
- **现象**：依据路径 `…/二、项目基本条件/9 律师事务所…/9-1 ….docx`，磁盘上实际在"一、参与主体情况"下，按路径直查 404。
- **修复**：`openMaterialPreviewResolved` 不再只信路径——先查材料清单精确命中，
  不中就**按文件名模糊兜底**（即走 `_findMaterialPath` 四级匹配）。

### 问题 7：一条依据混写多个文件 + 夹带核对提示
- **现象**：`未在当前项目材料库中找到《法律意见书、PUE测试报告；评估净值及决算总投资待人工核对》`。
- **根因**：AI 把多个文件名用“、”混写，还夹带“待人工核对”备注，整体当一个文件名去找必然落空。
- **修复**：`openRefByName` 按“、，,”拆开逐项尝试，**过滤“待人工核对/待补充/待确认”**，
  打开第一个能定位的真实文件，并 toast 说明“依据提及多个文件，已打开第一个可定位的：《…》”。

### 问题 8：依据能打开文件但不跳转高亮（摘录格式无“原文摘录”标记）
- **现象**：点依据后只弹出整个文件，不滚动到摘录句、不高亮。
- **根因**：该依据格式为 `路径〈摘录内容〉`（第一个〈…〉直接是摘录，**没有〈原文摘录〉标记词**），
  而解析只认 `〈原文摘录〉` 分隔格式 → quote 被解析为空 → 预览不定位不高亮。
- **修复**：抽出 `_splitPathQuote`，同时兼容两种格式：①有〈原文摘录〉按它切；②无标记词时第一个〈…〉即摘录。

### 问题 9：扫描件 OCR 文本夹空格/标点差异，摘录逐字匹配失败
- **现象**：摘录明明在原文里，却提示“未能逐字定位”。
- **根因**：扫描件预览文本来自 OCR/视觉识别，汉字间常夹杂空格换行，引号全角半角也可能不同，`indexOf` 逐字匹配落空。
- **修复**：`openMaterialPreview` 摘录定位升级为四级：①逐字匹配；②开头 6~14 字兜底；
  ③**忽略所有空白**的映射表匹配（去空白后 indexOf，再映射回原文下标高亮）；
  ④再把“”/（）/逗号句号等常见标点差异抹平后匹配。

---

## 三、当前最终匹配链（前端 app.js）

点击"📎 依据"后的完整解析顺序：

1. **拆条**：按 `〈数字〉` 拆多条，再按"；"拆（兼容旧格式）；
2. **分类**：
   - `申报材料：…` → `_splitPathQuote` 切出路径+摘录（兼容带/不带〈原文摘录〉标记两种格式）→ `openMaterialPreviewResolved`；
   - `同上文件` → 复用上一条路径 `_lastSrcMaterialPath`；
   - `摘要表：…` → 跳转摘要表字段行；
   - 无前缀裸文本（≤60字且非天眼查/网络/固定表述/planning）→ `openRefByName`；
3. **路径解析 `openMaterialPreviewResolved`**：材料清单精确命中 → 打开；否则按文件名模糊兜底；
4. **文件名匹配 `_findMaterialPath` 四级**：
   ① 精确同名 → ② 互含（全名）→ ③ 去扩展名主干互含 → ④ 编号前缀相同 + 核心词命中（扩展名优先）；
5. **`openRefByName` 多文件拆分**：按“、，,”拆、过滤核对提示、逐项匹配、打开第一个命中；
6. **摘录定位四级**（`openMaterialPreview`）：逐字 → 开头段 → 忽略空白（映射表回填高亮区间）→ 标点归一；
7. **都失败**：提示当前项目名 + 缺失文件名 + 补传指引。

---

## 四、预防措施（写给以后的开发/生成逻辑）

1. **src 标注必须来自真实路径**：后端 `skill_runner` 在 AI 调 `read_document` 时自动记录
   `申报材料：{真实path}` 为依据——这条链路最可靠。AI 手写 src 时容易简写/写错目录，
   前端所有兜底都是在为这个"擦屁股"。若以后改 prompt，应强调**照抄 list_materials 返回的相对路径**。
2. **长目录名会被 `_clip_name` 截断**（>200 字节加 `~md5[:6]` 后缀）：任何"按依据路径直查磁盘"的逻辑
   都必须带"按文件名模糊兜底"，不能只信路径字符串。
3. **新增报错文案规范**：对用户显示"哪个项目 + 缺什么 + 怎么办"，禁止直接抛技术错误信息
   （本项目面向金融用户，界面禁止展示技术细节）。
4. **批量自检脚本**（诊断用，可放服务器随时跑）：提取所有章节 src 比对材料库，
   输出命中/缺失统计——本轮就是靠它 1 分钟锁定"空项目"真相：

```python
import sqlite3, re, os, json
con = sqlite3.connect("/app/workspace/app/backend/database/reits.db")
root = "/app/workspace/projects/<项目ID>/materials"
files = [os.path.relpath(os.path.join(dp, fn), root).replace(os.sep, "/")
         for dp, _, fns in os.walk(root) for fn in fns]
base = lambda p: p.split("/")[-1]; names = [base(f) for f in files]
rows = con.execute("select chapter_n, data_json from generation_jobs where project_id=<项目ID>").fetchall()
for ch, dj in rows:
    if not dj: continue
    for m in re.finditer(r"申报材料[：:]\s*([^\s；;〈」]+)", dj):
        ref = m.group(1).replace("《","").replace("》","")
        nm = base(ref)
        hit = ref in files or nm in names or any(nm in n or n in nm for n in names)
        if not hit: print(f"缺失 [第{ch}章] {ref}")
```

---

## 五、相关文件索引

| 文件 | 职责 |
|---|---|
| `frontend/js/app.js` | `openSrcLink` / `openMaterialPreviewResolved` / `_findMaterialPath` / `openRefByName` / `openMaterialPreview` |
| `backend/services/skill_runner.py` | 生成时自动记录 `申报材料：{path}` 依据（`_add_ref`） |
| `backend/routers/projects.py` | `_clip_name` 长名截断、材料清单 API（files + dirs） |
| `backend/services/materials_client.py` | 材料读取：PDF 文字层 / 扫描件 OCR（Moonshot→本地 tesseract 兜底） |

## 六、依据链路失败率治理（全量自检 84 条）

用户反馈“点依据→打开文件→翻到页→红框”失败率太高。对测试case 全部 84 条申报材料依据跑全链路自检，失败分布与修复：

| 环节 | 治理前 | 治理后 | 修复手段 |
|---|---|---|---|
| 文件匹配 | 50/84（60%） | **81/84（96.4%）** | `_findMaterialPath` 加 ⑤任意位置编号（承诺函（12-1）/14-1号文件）⑥描述性子串词（各年度审计报告现金流量表）⑦多文件混写拆分 ⑧文件夹级依据（26 润泽发展所获荣誉及奖项→目录下第一个文件） |
| 文字层翻到页 | 3/6 | **6/6** | `_quote_page_hit` 片段投票：AI 摘录常是改写，逐字匹配太严→拆片段（含虚词拆专名）+连字符归一（A18 vs A-18），命中≥2 或单片段≥10字 |
| 扫描件搜页 | 9/15 有数字才搜 | **26/26 有特征词** | 无数字摘录改用中文片段（≥6字）搜页；`_norm_q` 归一空白/连字符 |
| 红框定位 | 仅数字行 | 数字或中文片段行 | `ocr_page_highlight_box` 匹配归一化，tokens 复用搜页同一套 |

剩 3 条失败均为 AI 生成时**臆造编号**（“20号专项税务意见”，磁盘 20 号是 PPP 不涉事项、税务实为 21 号），属生成质量问题，不强行猜配错误文件。

## 七、Git 提交链（本轮）

- `fix: 依据链接支持〈1〉〈2〉多条拆分+同上文件+原文摘录正确截取+路径失配模糊兜底`
- `fix: 依据链接找不到文件时提示当前项目名+缺什么+补传指引`
- `fix: 依据文件名匹配增加编号前缀+核心词规则-救回AI简写的文件名`
- `fix: 依据混写多文件名时拆开逐个定位-打开第一个可定位文件并过滤待核对提示`
- `fix: 依据兼容无原文摘录标记格式+摘录定位忽略OCR空格与标点差异`
- `feat: 扫描件摘录命中页红框标出具体位置-OCR词级坐标定位段落`
- `fix: 依据链路失败率治理-文件匹配加任意编号/描述子串/文件夹级规则+搜页改片段投票与连字符归一`
- `fix: 文字层PDF摘录也红框标出-search_for精确坐标+行级bbox兜底+坐标约定统一120dpi`（核实后文字层 6/6 有框、扫描件 26/26）
