# REIT-AI 申报材料生成系统 · 用户使用说明书

> 本说明书面向**系统使用者**。您无需安装任何软件、无需关心服务器与环境配置——**打开网址、登录、直接使用**即可。
> 账号开通、系统维护等事宜请联系管理员，不在本说明书范围内。

---

## 一、系统是做什么的

REIT-AI 是一个**法律申报材料智能生成平台**：

- 调用 **Kimi 大模型**，按官方格式模板**逐章自动生成** REITs 基金发改委申报材料（内置《REITs 发改委申报材料（2024 版）》模板包，共七章）；
- 生成后可在网页上**像 Word 一样在线编辑**：改正文、调表格、加脚注、插框图，还能用 **AI 辅助润色/改写/扩写**；
- 随时**实时预览**官方格式 Word 效果，确认后**一键下载各章 Word 文档**。

一句话：**上传证明材料 → 点"生成" → 人工核对润色 → 下载 Word**。

> ⚠️ **重要**：AI 生成的内容必须经人工逐段核对后再对外报送，系统不对生成内容的准确性负责。

---

## 二、开始使用

### 2.1 打开系统

在浏览器（推荐 Chrome / Edge）地址栏输入系统网址并回车，即可看到登录界面。**无需安装任何插件或软件。**

> 🌐 **系统网址：`http://193.112.194.61`**

### 2.2 登录

1. 输入管理员分配的**用户名和密码**登录；
2. 如果系统提示"首次登录请修改密码"，按要求设置新密码：
   **至少 8 位，且大写字母 / 小写字母 / 数字 / 符号至少包含三类**；
3. 请妥善保管自己的密码。忘记密码或账号被锁，请联系管理员处理。

账号相关规则：

| 规则 | 说明 |
|---|---|
| 登录保护 | 连续输错 5 次，账号锁定 15 分钟 |
| 登录有效期 | 约 **12 小时**，过期后重新登录即可，**数据不会丢失** |
| 数据隔离 | 每个账号只能看到和操作**自己的项目**，与其他人互不干扰 |
| 退出登录 | 点击页面左侧边栏底部的「**退出登录**」按钮 |

---

## 三、系统概览页（首页）

登录后默认进入"系统概览"，包含：

- **统计卡片**：项目总数、生成中、已完成、模板数；
- **项目列表**：展示您名下的所有项目，可进入或删除；
- **➕ 新建项目**：创建新的申报项目（见下节）。

### 3.1 新建项目

点击"➕ 新建项目"，填写：

| 字段 | 说明 |
|---|---|
| 项目名称 | 如"XX数据中心基础设施REITs" |
| 申报材料文件夹（可选） | 点"选择文件夹"，在**您自己电脑的文件夹选择器**中选取任意位置的文件夹（含桌面）。文件夹内的 PDF/Word/Excel/PPT/图片/zip 等文件会在创建项目后**自动上传**为申报材料。也可先不选，建好项目后到「系统设置 → 申报材料」再传 |
| 材料模板 | 选择要使用的模板包，默认 **REITs 发改委申报材料（2024 版）**。章节结构、写作要求、官方 Word 模板全部由所选模板包决定 |

创建成功后回到列表，**点击项目名即进入材料生成页**。

> 系统预置了一个**示范项目**供体验参考，示范项目不可删除；自己创建的项目可以随时删除（删除会同时清掉该项目已上传的材料和已生成的内容，请谨慎）。

---

## 四、核心流程：材料生成页

进入项目后是本系统的核心页面，从上到下依次为：**项目信息栏 → 章节步骤条 → 章节编辑区 → 高级选项（增强功能）→ 整体进度条**。

### 4.1 第一步：上传申报材料

进入 **系统设置 → 申报材料（当前项目）**，点 **⬆️ 上传材料**：

- 支持多选上传：PDF / Word / Excel / PPT / TXT / Markdown / CSV / 图片（png、jpg）/ **zip**；
- **zip 会自动解压**，里面的多级文件夹结构原样保留；
- 上传的材料属于当前项目，生成时 Kimi 会自动读取这些文件（承诺函、审计报告等），**据实填写**；
- 不上传也能生成，但需要看文件的地方会标注"**待核实**"；
- 同名文件重复上传会直接覆盖；"清空"按钮可删除该项目全部材料。

### 4.2 第二步：填写摘要表和释义

步骤条第一项是"**摘**"（摘要表和释义），点击进入后有三个可展开区块：

1. **摘要表**——项目关键信息（键值对）；
2. **释义**——术语简称与释义；
3. **其他基本信息**。

操作要点：

- 键、值均可直接编辑；支持**新增行、插入行、删除行、按住 ⠿ 拖动排序、↶ 撤销**；
- 可点 **📥 上传Excel导入**：上传一个含三个 sheet（摘要表 / 释义 / 其他基本信息）的 `.xlsx/.xls` 快速导入；
- 改完务必点 **💾 保存**——**之后各章生成都以这份摘要表为准**。

### 4.3 第三步：用 Kimi 生成章节

在步骤条点选某一章（如"一、项目基本情况"），编辑区右上角点 **🤖 用Kimi生成**：

- 生成为**后台异步任务**，一章约需**数分钟**，期间顶部会显示"Kimi 正在生成…"，可以切到别的章节继续操作；
- 生成完成后自动刷新编辑区并提示"生成完成，请核对编辑"；
- 同一章生成中不能重复启动；已生成的章节可点"**重新生成**"覆盖重来；
- AI 接口有**限流保护**（每账号每分钟有次数上限），提示"调用过于频繁"时稍等一分钟再试。

### 4.4 第四步：在线编辑（Word 式编辑区）

每个小标题下是一个可整块编辑的正文区，除直接打字外还提供：

| 功能 | 用法 |
|---|---|
| **表格编辑** | 光标点进表格单元格，表格上方自动出现工具条：插行/删行、插列/删列、合并右/合并下、拆分 |
| **➕ 脚注** | 先把光标放到正文要加注的位置，再点"➕ 脚注"输入内容；正文出现上标编号，编辑区下方同步显示脚注面板，可直接改/删；双击正文中的脚注编号也可编辑 |
| **🖼 画图** | 先把光标放到要插图的位置，再点"画图"，选模板（内置**产品结构图、股权结构图**，也可选空白），在内置 draw.io 画布中绘制，保存后图片插入正文；**双击图片可重新编辑**，点图片右上角 ✕ 删除 |
| **✨ AI辅助** | 先在正文**选中一段文字**，再点"AI辅助"（不选文字则按指令直接创作）：<br>① 快捷指令：**润色 / 改写更书面 / 扩写 / 精简 / 纠错**；<br>② 也可自输指令，并可"添加素材"：**粘贴文字 / 网页链接（每行一个）/ 上传文件**（Word、PPT、Excel、PDF、图片）；<br>③ 生成结果可再次编辑，然后**替换选区 / 插入到选区后 / 复制 / 重新生成** |

> 所有编辑操作（表格、脚注、插图、AI 替换）都**必须点 💾 保存**才会生效并写入项目数据。

### 4.5 第五步：预览与下载 Word

- **📄 开启Word预览**：编辑区右侧展开实时预览，内容会**写入官方格式模板**对应章节后渲染，所见即最终 Word 效果；保存内容后预览自动刷新；
- **⬇ 下载Word**：预览栏右上角，下载该章 Word 文件（命名如 `第1章_项目基本情况.docx`）；
- 若编辑区提示"未读到本章小标题"，说明模板包内置官方模板缺失，请联系管理员检查模板包配置。

### 4.6 高级选项——增强功能（六个 Tab）

页面下方"⚡ 高级选项 - 增强功能"折叠面板，面向细节完善：

| Tab | 作用 |
|---|---|
| **释义表** | 维护项目级术语释义（搜索、新增、保存），供全文统一引用 |
| **承诺函** | 选择内置承诺函模板，填写变量（主体名称、日期等）自动填充成文 |
| **财务数据** | 录入/维护项目财务指标，按标准模板格式化 |
| **不涉及** | 勾选本项目不适用的条款模块并填写理由，生成时按"不涉及"口径处理 |
| **基准日** | 设置评估/审计基准日与查询时点，统一全文口径 |
| **附件** | 维护附件编号清单，供正文自动引用"附件X" |

---

## 五、文档管理页

左侧导航"文档管理"列出**当前项目已生成的各章 Word 文档**（文件名、生成时间、大小），可直接点**下载**。
若列表为空，说明还没有章节完成预览/生成，请回到材料生成页操作。

---

## 六、系统设置页

| 设置项 | 说明 |
|---|---|
| **Kimi 模型** | 下拉列出可用模型，选择后**即时生效**，无需重启。写申报材料建议使用最新旗舰通用模型（如 kimi-k3） |
| **申报材料** | 上传/查看/清空当前项目的证明材料，见 [4.1](#41-第一步上传申报材料) |

> 关于修改密码：系统仅在**首次登录时**强制修改初始密码，之后界面不提供自助改密入口；如需修改或重置密码，请联系管理员。

---

## 七、标准工作流（一页速览）

```
① 浏览器打开系统网址 → 登录
② 概览页"新建项目"：填名称、（可选）选择您电脑上存放材料的文件夹、选材料模板
③ 创建后文件夹内的材料自动上传（也可稍后到系统设置补传，zip 可直接传）
④ 材料生成页 → 步骤条"摘"：填写/导入摘要表和释义 → 保存
⑤ 逐章点"🤖 用Kimi生成"，等待数分钟
⑥ 核对正文：改文字 / 调表格 / 加脚注 / 插图 / ✨AI辅助润色 → 💾 保存
⑦ 📄 开启Word预览核对版式 → ⬇ 下载Word
⑧ 文档管理页统一下载各章成品，人工复核后报送
```

---

## 八、常见问题（FAQ）

**Q1：打不开网页？**
确认网址输入正确（http://193.112.194.61）、网络连通；仍打不开请联系管理员确认服务状态。

**Q2：忘记了密码？**
请联系管理员重置。

**Q3：提示"未登录或登录已过期"？**
登录态约 12 小时有效，重新登录即可，**数据不受影响**。

**Q4：登录提示"失败次数过多，账号已锁定"？**
连续输错 5 次会锁定 15 分钟，等待自动解锁或联系管理员。

**Q5：看不到别人的项目 / 别人看不到我的项目？**
这是正常的。系统按账号隔离数据，每个账号只能看到和操作自己的项目。

**Q6：生成章节一直显示"生成中"或提示失败？**
- 一章通常要数分钟，请耐心等待；
- 提示失败多为网络或大模型服务波动，重新点"用Kimi生成"重试；
- 提示"调用过于频繁"是限流保护，等一分钟再试。

**Q7：生成内容里出现"待核实"？**
说明该处需要读取证明材料，但项目还没上传对应文件（或文件里找不到）。到系统设置上传材料后重新生成该章即可。

**Q8：编辑了内容但刷新后没了？**
编辑区的修改必须点"💾 保存"才会写入；未保存的内容在页面刷新后会丢失。

**Q9：可以同时开多个标签页用吗？**
可以，但请**避免在两个标签页同时编辑同一章**，后保存的会覆盖先保存的。

**Q10：下载的资料安全吗？**
生成的 Word 里包含项目敏感信息，请注意保管，不要随意上传到公开网盘。

---

## 九、注意事项

1. **AI 结果必须人工复核**：大模型可能出错或遗漏，报送前请逐段核对关键数据（金额、主体名称、日期、证照编号等）。
2. **材料越全，生成越准**：承诺函、审计报告、证照等证明材料上传得越齐全，"待核实"越少。
3. **改完就保存**：编辑区的所有修改（含表格、脚注、插图、AI 替换）都要点"💾 保存"才生效。
4. **摘要表先填好**：摘要是各章生成的基准，建议最先完成并保存。
5. **账号不外借**：账号对应您名下的项目数据，请勿与他人共用账号。

---

*本说明书面向系统使用者编写；部署、运维与账号管理事项请联系管理员。如界面与说明不一致，请以系统实际版本为准。*
