# REIT-AI-System 问题核实报告

> 核实日期：2026-08-05
> 核实方式：逐文件阅读源码 + 全局检索取证（非计算机专业人员编写的代码，以下每一条均有代码实证）
> 核实范围：`REIT-AI-System/REIT-AI-System`（前后端）+ 外层 `skills/` 目录

---

## 一、通用性核实：系统是"通用网站"还是"REITs 专用"？

**结论：当前"REITs 基金发改委申报材料"这套业务不是一个可插拔模板，而是烧死在引擎各层里。要做成通用网站，必须先做"引擎 / 模板 / 项目数据"三层分离。**

### 1.1 业务结构写死在引擎代码里

| 位置 | 证据 | 性质 |
|---|---|---|
| `backend/services/skill_runner.py` 第 22~30 行 | `CHAPTERS` 字典把七章标题（"一、项目基本情况"…"七、募集资金用途情况"）硬编码在 Python 代码里 | 业务结构写死在引擎，新增一种材料类型必须改代码 |
| `backend/mappings/*.json`（7 个文件） | 章节字段映射、附件清单、承诺函措辞库、财务指标规范、术语表、表格 schema 全部是 REITs 发改委 2024 版这一套 | 模板资源与引擎混放 |
| `backend/templates/official/ndrc_2024.docx` | 内置官方模板仅此一份，作为各章小标题/预览/导出的兜底 | 模板与引擎耦合 |
| `backend/config.py` 第 45~48 行 | `NDRC_MATERIALS_PATH`、`CSRC_MATERIALS_PATH` 等业务路径写死在配置里 | 业务概念渗入基础配置 |

### 1.2 具体项目的真实内容混进了"写作规则"

| 位置 | 证据 | 性质 |
|---|---|---|
| `skills/reits-reading-ch2/SKILL.md` 第 215~221 行 | 写死了**奥飞数据中心项目**的承诺函原文（"国浩律师(广州)事务所…截至2026年5月29日…"等四段，含真实日期、真实机构名） | 某项目的真实内容污染了通用写作规则 |
| `skills/reits-reading-ch3/SKILL.md` 第 33 行 | 表格示例行写死"奥飞数据" | 同上 |
| `skills/reits-reading-ch1/ch1.json`、`ch2/ch2.json`、`skills/summary_saved.json`、`skills/ch1_content_generated.json`、`skills/ch1_content_saved.json` | 真实项目的生成产物（摘要表、章节内容）散落在规则目录里 | "规则"与"项目数据"没有分家 |
| `backend/services/skill_runner.py` 第 398、406、442、793 行 | 注释中直接引用"1.奥飞数据""2.固安聚龙"作为示例 | 危害小，但说明开发者脑子里只有这一个项目 |

### 1.3 演示数据冒充真实数据

| 位置 | 证据 |
|---|---|
| `backend/data/projects/wanguo_idc_reit.json` | "万国数据"示范项目启动时自动导入数据库（`db.py` 的 `load_preset_projects`） |
| `frontend/index.html` | 项目列表、最近任务时间线、文档管理表格、版本历史全是硬编码的"万国数据"假数据（真实数据要靠 JS 拉 API 后覆盖，API 失败时用户看到的就是假数据） |
| `frontend/js/enhancements.js` | 检索命中项目名，演示数据与真实逻辑混杂 |

### 1.4 通用化目标形态（建议）

```
引擎（通用，不认识"REITs"）：skill_runner / web_render / 工具调用 / 在线编辑器
模板包（可插拔，一种材料一个包）：templates-packs/reits-ndrc-2024/
   ├─ template.docx        官方 Word 模板
   ├─ chapters.json        章节结构定义（替代写死的 CHAPTERS）
   ├─ planning.md          全局总纲
   ├─ reading-ch1~7.md     各章写作要求（不含任何具体项目内容）
   └─ write_config.json    排版要求
项目实例（用户数据，按项目隔离）：summary、ch{n}.json、上传的证明材料
```

---

## 二、上线核实：从"本地 8000 端口"到公网部署，需要做什么

**结论：整套设计就是按"单机单用户"写的，上线前有以下必须项。**

### 2.1 必须修改（不改无法安全上线）

| # | 问题 | 代码位置 | 改法 |
|---|---|---|---|
| 1 | 只绑定本机回环 | `backend/config.py` 第 60 行 `APP_HOST = "127.0.0.1"` | 改 `0.0.0.0`，前置 Nginx + HTTPS |
| 2 | 前端 API 地址写死 | `frontend/js/api.js` 第 5 行 `API_BASE = 'http://127.0.0.1:8000/api'` | 改相对路径 `/api`（否则部署后浏览器访问的是用户自己电脑） |
| 3 | **没有任何登录认证** | 全部路由均无鉴权依赖 | 任何人打开网址即可调所有 API，必须加认证（如 JWT/Session） |
| 4 | **任意路径浏览接口** | `backend/routers/folders.py` 第 129 行 `/folders/browse-any`，注释自述"不做路径穿越限制" | 本机合理，公网等于敞开服务器磁盘；必须删除或加严格鉴权+路径白名单 |
| 5 | **"本机路径"模式整体不成立** | 系统设置的模板/申报材料路径存浏览器 localStorage（`app.js` 第 22~38 行），后端按该路径直接读文件 | 多用户各有文件，必须改成"上传到服务器、按用户/项目隔离存储"（工作量最大的一项） |
| 6 | **单用户全局状态** | `routers/skills.py` 的 `_jobs`/`_tasks`/`_PREVIEW_CACHE` 内存字典；`skills/summary_saved.json`、`ch{n}.json` 全局唯一一份；SQLite 单文件 | 两人同时用会互相覆盖；需按"用户+项目"维度隔离（DB 加 user_id/project_id，文件按项目分目录） |
| 7 | CORS 全开 | `backend/main.py` 第 44~50 行 `allow_origins=["*"]` 且 `allow_credentials=True` | 收敛到正式域名 |
| 8 | 密钥管理 | `.env` 中 `MOONSHOT_API_KEY`、`TIANYANCHA_MCP_KEY` 多人共用一个 Kimi key | 上线后改环境变量注入，并考虑额度控制/防刷 |

### 2.2 建议修改

- uvicorn 单进程 → 多 worker（前提：生成任务状态从内存挪到 Redis/DB）。
- Docker 打包（PyMuPDF、lxml 等含原生依赖）。
- 上传的证明材料放对象存储或专用数据卷。
- 补 CI（至少跑 lint + 启动冒烟）。

---

## 三、代码规范核实：实证 bug + 工程规范问题

**结论：不止风格问题，存在前后端接口对不上的实证 bug——"增强功能"6 个 tab 中至少 4 个是坏的。**

### 3.1 实证 bug：前后端接口不匹配

对照 `frontend/js/enhancements.js` 与 `backend/routers/enhancements.py`：

| 前端调用（enhancements.js） | 后端实际（enhancements.py） | 后果 |
|---|---|---|
| 第 32/35 行：`GET/PUT /projects/{id}/financial` | 第 169/185 行：`/financial-data` | 财务数据 tab **必然 404** |
| 第 43 行：`PUT /inapplicable` 发 `{section_ids, reason}` | 第 219 行：收 `{sections, reason}` | 不涉及标记**保存必失败** |
| 第 51 行：`PUT /query-dates` 发 `{dates}` | 第 264 行：收 `{base_date, query_point, extra}` | 基准日**保存必 400** |
| 第 16/19 行：`POST /glossary`、`DELETE /glossary/{entryId}` | 后端**不存在**这两个路由 | 增删释义条目必失败 |

### 3.2 工程规范问题

1. **没有 git 仓库**（项目根无 `.git`）、**零测试**（全库无任何 `test_*.py`）、无 lint/格式化配置、无 README（只有面向非技术人员的《使用说明-请先读我.txt》）。
2. **两套管线并存**：旧管线（`routers/projects|chapters|generate.py` + `generators/` + `managers/` + `mappings/` + `parsers/`）与新管线（`routers/skills.py` + `services/skill_runner.py` + 外层 `skills/`）功能重叠，大量死代码；`index.html` 里部分按钮仍挂旧视图。
3. **前端演示假数据硬编码**在 HTML 里冒充真实数据（见 1.3）。
4. **单文件多职责**：`backend/services/skill_runner.py` 一个文件 1044 行，JSON 解析修复、HTML↔结构化互转、prompt 拼装、三类工具定义、表号重排、write_config 生成全在里面。
5. **运行垃圾与源码混杂**：`__pycache__/`、`server_out.log`、`server_err.log`、`.deps_installed` 与源码同目录；`run.bat` 与 `启动网站.bat` 功能重复。
6. **异常处理粗糙**：大量 `except Exception` 直接吞掉或转 500，不利于定位问题。
7. **数据库连接无池化**：每个请求都 `aiosqlite.connect` 新建/关闭连接（`db.py`）。
8. **模型定义重复**：`models/project.py`、`models/chapter.py` 与路由内联的 Pydantic 模型重复定义，未复用。

---

## 四、建议推进顺序

| 阶段 | 内容 | 说明 |
|---|---|---|
| 第一步 | 修实证 bug + 工程化打底 | 修 3.1 四处接口不匹配；初始化 git + `.gitignore`；清理 `__pycache__`/日志/重复 bat；删除前端硬编码假数据。小快赢，风险低 |
| 第二步 | 三层分离（核心重构） | 引擎 / 模板包 / 项目数据分离，REITs 发改委变成第一个模板包——这是"通用网站"的地基 |
| 第三步 | 多用户化 + 上线 | 认证、按用户隔离数据、上传替代本机路径、删除或收敛 `browse-any`、CORS 收敛、HTTPS、Docker 部署 |

---

*本报告由代码逐行核实生成，所有条目均可按"文件:行号"回溯验证。*
