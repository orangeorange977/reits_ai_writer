# Archive 同事项目调研：值得学习并改进的部分

> 调研日期：2026-08-11
> 调研对象：`Archive/reits-proof-extractor-robust`（证明材料提取端）+ `Archive/reits-application-generator-robust`（申报材料生成端）
> 对照对象：本系统现行管线（templates-packs/reits-ndrc-2024 + skills/reits-reading-ch1~7 + reits-writing + backend）
> 结论性质：只调研不改码；所有判断均来自双方真实源码核对，非推测。

---

## 一、两套管线的架构差异（背景）

| | Archive 方案（同事） | 本系统现行方案 |
|---|---|---|
| 形态 | 纯 Agent Skill（命令行批处理，无界面） | Web 产品（FastAPI + 前端，多用户多项目） |
| 结构 | 两段式：**提取端**全量读材料 → `extracted_data.json` → **生成端**过契约门禁后填 Word | 逐章式：ch1~ch7 reading 技能各产一份结构化 JSON → `web_render.py` 写入官方模板 |
| 人机协作 | 无，一次性批跑 | 网页编辑、AI 辅助改写、依据点击溯源 |
| 质量重心 | 生成前的硬门禁体系（覆盖率/契约/勾稽） | 生成后的依据纠偏（verify_fix_refs）+ 人工预览 |

**核心判断**：对方不是"更好的替代方案"，而是在**质量工程（防漏、防错、防失控）**上做得非常重，这正是本系统目前偏弱的环节。应借机制、不借代码。

---

## 二、值得借鉴的 6 个机制（按价值排序）

### 1. 缺件检测 + 材料台账 ⭐⭐⭐（价值最高、成本最低）

**对方做法**
- `scripts/check_missing.py` + `templates/standard_proof_catalog.json`：内置 25 项法定证明材料清单。
- 材料展开后立刻与清单比对，输出 `missing_materials.json` 缺件清单；缺件不阻断提取，但强制进入问题清单。
- `scripts/scan_proofs.py` 建立全量文件台账 `proofs_index.json`（文件清单 + 材料编号）。

**本系统现状**
- `backend/routers/projects.py` 的 `upload_materials` 只负责存文件（zip 解压、建目录）。
- 全代码库检索无任何缺件/清单比对逻辑。用户上传少了什么材料，要等到某章生成时 AI 找不到文件才暴露，且不会显式提示。

**改进建议**
- 上传完成后按 25 项清单做一次目录名/文件名语义比对，前端提示"缺 X 项材料：……"。
- 对方的 `standard_proof_catalog.json` 可直接作为清单配置参考。
- 成本：一个后端比对函数 + 前端提示条，不动生成链路。

---

### 2. 覆盖率台账：量化"到底读了哪些材料" ⭐⭐⭐

**对方做法**
- 每读一页/一个文件就登记到 `extracted_data.json` 的 `_metadata.read_items`，"先原子落盘数据，再登记已读页"。
- `pipeline_state.py` **现算**文件覆盖率/页覆盖率（不采信任何自报状态），核心报告（审计、评估、法律意见书、营业执照、权属证照）要求 **100% 文件 + 100% 页覆盖**。
- 其源码注释记录了实战教训："读了评估报告前 3 页摘要就 PASS——实测导致表 4-4~4-15 全是空表"。

**本系统现状**
- 一章生成是黑盒一次性调用（`skill_runner.run_chapter` → Kimi 工具循环），生成完**没有任何"实际读了哪些文件/页"的记录**。
- 表格空了、数据缺了，只能靠人工预览发现。

**改进建议**
- 让"读取申报材料"工具每次被调用时，顺手把"文件相对路径 + 页码"登记到项目目录的一个台账文件（如 `workspace/projects/<id>/read_ledger.json`）。
- 章节生成完成后，结果里附带"本章查阅材料 N 份/共 M 份"；对财务类章节（ch2/ch4）可进一步校验"对应年度财报文件必须出现在台账中"。
- 成本：工具执行器里加几行登记代码 + 生成结果展示一行统计。

---

### 3. 表格行模型 + 自动勾稽校验 ⭐⭐⭐

**对方做法**
- `templates/handoff_contract.json` 把第四章表 4-1~4-15、第二章表 3~10、第五章表 15~22 逐表映射到结构化数据源路径（行模型：`annual_rows`、`forecast_rows`、`revenue_structure_rows` 等），表格由蓝图脚本确定性生成，AI 不得手写表格。
- 强制校验：比率类指标（资产负债率、70% 测试）**从基础值重算**；检查重复行标签、列数漂移、期间错位、单位缺失。

**本系统现状**
- 第二章财务表等由 AI 直接读财报填数（ch2 SKILL.md 中以自然语言指令描述"资产负债率=总负债/总资产×100"），算错、取错期间、单位不一致均无自动校验。
- `web_render.py` 只负责按 JSON 重建表格，不校验数据合理性。

**改进建议**
- 不必搬对方 125KB 的蓝图引擎。做一个**轻量后置校验器**即可覆盖最高频硬伤：
  - 对 ch{n}.json 中含财务数字的 grid 表，重算比率列与已知公式（资产负债率、EBITDA 勾稽）；
  - 核对期间表头与列数一致、金额单位标注存在、同一指标跨表数值一致。
- 校验不过 → 在章节预览中对该表标黄提示，不阻断保存。

---

### 4. 阶段间硬门禁 + 报告新鲜度检查 ⭐⭐

**对方做法**
- `handoff_gate.py`（仅 55 行）：写 Word 前强制检查交接报告为严格 `READY`；且用 **mtime 比对**确认校验报告晚于全部数据文件，防止"拿旧校验结论放行新数据"。
- 生成端所有写入脚本共用这一联锁，校验不过直接 exit。

**本系统现状**
- `chapter_preview` / 装配流程拿到章节 JSON 就直接写 Word，无前置体检；JSON 结构坏块、表格列数错乱会直接写进 docx。

**改进建议**
- 写 Word 前加一个 JSON 健康检查门禁：块结构合法、kv/grid 列数一致、"表#"占位规范、依据字段格式合规。
- 不合格项在预览页列出（业务语言，如"第 X 节表格列数与表头不一致"），而不是静默硬写。

---

### 5. 断点续跑：状态从磁盘产物推导 ⭐⭐

**对方做法**
- `pipeline_state.py` 设计要点："checkpoint 是**推导产物不是真相**，随时可由脚本重建，禁止手工编辑"；会话中断/换人接手后跑一条命令即可恢复，不依赖对话记忆。
- 批次粒度推进："取下一批 → 处理 → 落盘 → 登记"，任何一步失败只损失一批。

**本系统现状**
- 章节生成失败即整章重跑（`_run_chapter_job_with_subs` 捕获异常后置 error 即止），DB 只存任务状态，不存中间产物。
- 一章内容多、耗时长（数分钟），整章重跑成本高。

**改进建议**
- 参考"小节级落盘"：按模板小标题把一章拆成若干小节分批生成，逐批合并落 `ch{n}.json`；失败后续跑时跳过已有小节。
- 至少先做到：失败时保留已完成部分 + 明确告知"已完成 X/Y 小节"。

---

### 6. 双遍阅读法（事实提取 + 遗漏审计） ⭐（零代码成本）

**对方做法**
- 每批材料跑两遍：第一遍提取事实、数字、日期、条款、表格原貌；第二遍**只做遗漏审计**——核对标题、脚注、续表、单位、合计行、负号、百分号、期间，不重写第一遍结果。

**改进建议**
- 纯 prompt 技巧：把"第二遍只做核对，重点看续表/单位/合计/负号"写进各章 reading SKILL.md（尤其 ch2/ch4/ch5）。
- 成本为零，对表格密集章节收益明显。

---

## 三、不建议搬的部分

| 内容 | 不搬的理由 |
|---|---|
| 对方的代码本体（`fill_docx.py` 158KB、`gen_phase_fill_plan.py` 125KB、`validate_output.py` 90KB 等单体脚本） | 为"约束 agent 纪律"而写，与本系统 Web 架构不兼容，搬进来是维护负债 |
| 阈值防篡改红线（阈值只许调高、`--force-low-coverage` 越权留痕） | 防的是"agent 自改门槛"场景；本系统是产品化流程，无此风险面 |
| 第三/七章留白不生成的策略 | 本系统 ch3/ch7 reading 技能已覆盖，无需倒退 |
| 提取端/生成端双技能拆分 | 本系统逐章 reading→writing 解耦已达成同样效果，且粒度更利于单章返工 |

---

## 四、本系统已有、对方没有的长板（保持，不倒退）

- **依据点击溯源完整链路**：`verify_fix_refs` 生成后纠偏（路径归一到磁盘真实文件、摘录替换为原文原句、无原文条目整条删除）+ 扫描件搜页 + 页图高亮预览 + 天眼查可点击官网网址。对方只有静态 `_raw_text` 字段，无可交互溯源。
- **人机协作闭环**：网页编辑保存、AI 辅助改写（ai-edit/ai-compose）、摘要表/释义作为跨章共享变量（planning.md 机制）。对方纯批处理，错了只能重跑。
- **内容与写入解耦**：reading→JSON→确定性写入的拆分与对方理念一致（`assemble.py` 头部注释记录了早期耦合写入的真实事故），架构方向正确。
- **多用户多项目隔离、模型可选、模板包机制**：对方完全没有产品化能力。

---

## 五、落地优先级建议

| 优先级 | 改进项 | 预估改动面 | 依赖 |
|---|---|---|---|
| P0 | 缺件检测 + 上传提示 | 后端一个比对函数 + 前端提示 | `standard_proof_catalog.json` 可直接参考 |
| P0 | 双遍阅读法写进 reading SKILL.md | 仅改技能文档 | 无 |
| P1 | 材料阅读台账（read ledger） | 材料读取工具执行器 + 章节结果统计展示 | 无 |
| P1 | 财务表勾稽校验器 | 新增后置校验模块，接入章节预览 | 明确各表公式清单 |
| P2 | 写 Word 前 JSON 健康门禁 | web_render/preview 入口加校验 | 无 |
| P2 | 章节小节级断点续跑 | 生成循环改造 + 中间产物落盘 | 章节任务调度改造 |

**一句话结论**：借四个质量机制——**缺件检测、阅读覆盖率台账、财务表勾稽校验、写 Word 前 JSON 门禁**——补上本系统"生成即交付、质量靠人眼"的缺口；全部以轻量方式嵌入现有章节管线，不引入对方大脚本。

---

# 实施路线图：从哪里开始改 + 每步如何验证

> 补充于 2026-08-11。动手顺序原则：**先零风险，后纯增量，最后才碰生成主链路**；每完成一步先验证再进下一步。
> 已核实的事实依据：对方 25 项清单（`Archive/reits-proof-extractor-robust/templates/standard_proof_catalog.json`）结构清晰可直接移植，其分类一~五与 planning.md 定义的"1~5 号材料文件夹"编号体系天然对应；项目已有真实生成产物可做测试夹具（`workspace/projects/1/ch1.json`、`ch2.json`）；本地验证通道可用（`run_server.py` 起服务 + curl 调 API）。

## 阶段 0：双遍阅读法（半天，零代码）

**改什么**：只改 `templates-packs/reits-ndrc-2024/reading/` 下的 ch2、ch4、ch5 章节指南，追加"第二遍只核对：续表、单位、合计行、负号、百分号、期间，不重写内容"。

**怎么验证**：
1. 同一个项目、同一个模型，改动前后各生成一次第二章；
2. 写对比脚本统计三件事：财务表单元格非空率、单位标注（"万元/%"）出现次数、合计行是否保留；
3. 人工抽查 2~3 张表与原财报核对。

**为什么排第一**：零回归风险；且这一步先建立起"改前改后对比"的验证方法，后续每一步复用。

## 阶段 1：缺件检测（1~2 天）⭐ 代码从这里开始

**改什么**（三步，均为新增，不动现有逻辑）：
1. 把 25 项清单移植为 `templates-packs/reits-ndrc-2024/catalog.json`（放进模板包，跟随模板包机制走）；
2. 后端新增纯函数模块：输入材料目录树 → 输出"已匹配项/缺件项"。匹配策略两层：文件夹名按"1 发起人…""2 财务报表…"编号精确匹配；文件级按关键词匹配（营业执照、审计报告、承诺函、法律意见书、信用记录…）；
3. `list_materials` 接口附带返回缺件列表，前端材料页顶部提示（业务语言，如"缺少：发起人承诺函（第4项）"，不暴露技术细节）。

**怎么验证**（四层）：
1. **单元测试**：构造 fixture 目录——完整包、缺 2 项、文件名变体（"审计报告2023.pdf"、"奥飞公司承诺函.pdf"）、空目录，断言比对结果全对；
2. **真实数据零误报**：拿一份真实上传的材料目录跑一遍，已存在的 25 项必须全部识别为"已有"（误报 = 失败）；
3. **负向测试**：从副本里删掉某份承诺函，必须被精确报出；
4. **端到端**：本地 `run_server.py` 起服务，curl 调 `list_materials` 看返回结构，再开前端看提示条。

**前置条件**：项目 1 当前没有 materials 目录，真实数据回归需要一份真实材料包（重新上传或提供副本）；否则先用 fixture 验证逻辑，真实包上线前补测。

## 阶段 2：阅读台账（1 天）

**改什么**：材料读取工具每次被调用时，把"文件路径+页码"追加写入 `workspace/projects/<id>/read_ledger.json`；章节生成完成后附带统计（"本章查阅材料 N 份/共 M 份"）。

**怎么验证**：
- 生成一章后，台账条目数 ≥1 且与 `server_out.log` 里的工具调用记录逐条对得上；
- 生成耗时前后对比，确认台账写入不拖慢生成（误差应在秒级以内）。

## 阶段 3：财务表勾稽校验器（2~3 天）

**改什么**：新增独立校验模块，先只做三条规则——资产负债率重算、期间表头与列数一致、同一指标跨表一致；接入章节预览，问题标黄提示（不阻断保存）。

**怎么验证**：
1. **正例**：拿 `workspace/projects/1/ch2.json` 真实数据喂进去，应零误报（若报出问题，先人工确认是不是真问题——真问题反而证明有效）；
2. **注入测试**：脚本复制一份 JSON 分别注入三种错误（改错比率值、交换两列期间、删单位标注），三种都必须被抓出；
3. 前端预览确认提示以业务语言展示。

## 阶段 4（暂缓）：JSON 门禁 + 小节级断点续跑

这两项要动生成主链路，风险高于前三项；等阶段 1~3 验证跑顺后再启动。

## 贯穿全程的回归底线

每个阶段完成后统一跑一遍回归：**项目 1 的 ch1/ch2 能正常打开预览、编辑保存、下载 Word**，确认旧功能零破坏。所有验证脚本写成可重复执行的临时脚本（项目已有 `.tmp_*` 先例），验证完清理。
