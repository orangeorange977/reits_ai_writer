# 工作周报 · 2026年8月20日（周四）

> 统计周期：2026-08-20，承接《周报-2026-08-14-08-18》（08-19 详见当日日报）。依据：提交 afc51e9（10:28）、下午未提交改动 8 文件（+773/-117）、文件时间戳与生产部署记录交叉验证。

## 本期工作汇报

今天的工作重心，是把昨天模型切换实测中暴露的生成质量问题逐项根治，同时把系统里"画图靠人工"这个环节升级成全自动生成。概括讲就是两件大事：生成可靠性治理，和 AI 示意图自动生成管线落地。

**一是完成了生成可靠性的系统性治理，三类真实故障全部定位到根因并修复验证。**昨天实测暴露的空表、编造、取数不稳问题，今天逐一拆解：第一类是"表格被填成取数说明"——追查发现模型生成长章节时一轮会发起近百次工具调用，系统默认只允许 16 轮，经常在关键的审计报告还没读到时就被强制交卷。解决办法是把轮数上限提到 40，并在第二章写作规则中增加"取数优先级红线"，要求开工第一件事先读财报、禁止反复重读同一文件浪费轮次。第二类是"扫描件识别不稳导致整表放弃"——130 页的扫描版审计报告，同一页两次识别结果可能差出几倍（总资产一次读成 5.4 亿、一次读成 23.4 亿），原先"逐字摘录"的严格规则反而让模型一遇到不一致就整表填"待核实"交卷。今天把规则升级为"交叉核对"：数值不一致时必须重读一到两次、按多数一致原则定值并标注人工核对提示，只有关键指标反复核对仍不定才允许单格留空，严禁整表放弃；配套把财报的读取次数上限从 2 次放宽到 6 次。修改部署后用第二章实测重跑，四个年度的营收、净利润、现金流全部填出了真实数值，此前的整表"待核实"问题不再出现。第三类是"摘要表字段为空就占位交卷"——在第一章规则中明确：已保存的摘要表只是辅助参考，字段空缺时必须照常阅读申报材料自行提取，不允许以字段为空为由输出占位内容。至此，昨天实测暴露的三类可靠性问题形成了"系统参数 + 写作规则"双层修复，且都有部署后重跑的实测证据。

**二是 AI 示意图自动生成管线落地，第七章资金流向图已在生产环境验证通过。**此前申报材料里的股权结构图、产品架构图、资金流向图都要靠人工整理清单再单独画图，今天把这个环节改造为：模型直接输出结构化的图数据，系统自动渲染成图片插入 Word。渲染器支持了申报材料图示的全部特殊形态——侧挂主体（税务机关、募投项目挂在主链两侧不挤偏布局）、同向平行双箭头（如实缴出资与股东借款并行）、虚线框、椭圆框、分组框和图例，并重构了标签避让算法保证图面不重叠。生成侧把渲染结果物化成系统既有的图块格式，编辑区、校验门禁、Word 导出、预览四条链路完全复用现有机制、零改动；单张图渲染失败只丢弃该图，不影响整章生成。第二章法律关系图、第三章产品架构图、第七章资金流向图三处写作规则已同步改造，并强制要求图中的机构名称、持股比例、金额与正文和表格完全一致。第七章已完成生产部署并热渲染验证成功，第二、三章待端到端回归。

**此外还有两项收尾工作。**一是对照官方格式 PDF 全面重写了第一章写作规则（新增约 190 行），统一了项目概况的要素口径、表题占位规则和术语口径；官方《第二章.pdf》也已入库支撑规则比对。二是把昨天的全部工作连同今晨改动一次性提交入库（30 个文件，净增约 1770 行），并归档了上周周报和近三天日报。

## 做得好的地方

今天的可靠性治理没有停留在表面修补：三类故障都追到了真正的根因——"表格填成取数说明"查到的是工具调用轮数预算不够，而不是模型不听话；"整表待核实"查到的是识别不稳叠加规则过严，于是规则从"逐字摘录"改成了更符合实际的"交叉核对"，而且每项修复都用第二章实测重跑拿到了验证结果，不是改完就算。示意图管线的设计也比较克制：渲染结果直接物化成系统既有的图块格式，编辑、校验、导出、预览四条链路一行代码没动就全部兼容，单图失败也不影响整章生成。

## 需要说明的事项

下午的 8 个文件改动（示意图管线和红线升级，+773/-117 行）尚未提交，待第二、三章示意图端到端回归后一并入库。另外提请注意：近两日生产环境累积了多轮热部署改动，容器重建会将其抹掉，计划先完成本地提交再走正式发版流程整体上线。

## 下一步计划

第一，完成第二、三章示意图的真实项目生成回归，随后提交全部改动并正式发版；第二，在 deepseek-v4-pro 主力模型下持续观察新红线对其余章节生成质量的效果，MiniMax 保留为已接入的可选厂商；第三，清理今日的临时验证脚本，并确定官方参考 PDF 等资料的入库留档方案。

## 时间线速览

| 时间段 | 主题 |
|--------|------|
| 09:16–09:27 | 第一章规则重写、第二章取数优先级红线、官方《第二章.pdf》入库 |
| 10:26–10:28 | 昨日 + 今晨工作提交入库（afc51e9，30 文件） |
| 13:05–13:17 | 扫描件交叉核对红线、财报读取次数放宽、第一章信息源红线 |
| 16:37–16:52 | 示意图管线收尾（侧挂/平行箭头/标签避让），第七章生产热渲染验证 |

**今日小结：生成可靠性三类故障根因修复并实测验证通过、AI 示意图自动生成管线落地（第七章已生产验证）、第一章规则全面重写、昨日工作全部提交入库；另有 8 文件改动进行中待回归提交。**
