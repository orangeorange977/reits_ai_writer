# 工作日报 · 2026年8月13日（周四）

> 数据来源：当天 1 条 git 提交（09:12）+ 工作区 5 处未提交改动（09:55–13:23，其中 4 处已发版生产）+ 生产事故处置与成本治理操作记录（09:10–13:40，服务器时间戳/容器日志为证）

## 一、今日已完成事项（按主题）

### 1. reading-ch2 EBITDA 取数强化（09:12 提交）
- 第二章写作要求强化：EBITDA 须翻页查找审计报告后附的现金流量表补充资料（通常位于附注末尾），确在全文缺失才标【注】，不得未找先注

### 2. 生产事故处置：欠费复活任务写坏章节（09:10–10:10）
- 事故：Kimi 欠费期间卡死的生成任务在充值后自动复活，用损坏上下文于 09:10–09:44 写坏第一/二/三章（表全空、目录乱序、第三章仅剩骨架），用户侧表现为"一/二/三章目录全乱"
- 处置链：重启容器清掉在跑任务 → ch1/ch2 从 08-12 14:15 备份恢复 → ch3 骨架移入隔离目录（/home/ubuntu/quarantine_0813/）回到"未生成" → 逐章核对 ch4–ch7 与备份逐字节一致，零数据丢失
- 二次污染拦截：09:59 用户触发的重跑再次写坏 ch1 后，立即快照隔离并再次恢复填充版，确认恢复态目录正确、表 1 有值

### 3. 串章根因修复全量发版（09:55–10:10）
- 根因定位：历史前端切章竞态把第三章小节串存进第一章（第六章同病），而生产跑旧代码、无串章过滤，AI 重跑又照坏文件模仿
- 修复发版：前端按 DOM 实际章节号落盘杜绝串章；后端新增串章过滤（他章小标题剔除）+ 模板小标题强约束（sections 必须与模板一字不差）；数据侧剔除 ch1/ch6 各 5 个串章节
- 配套：业务日志开 INFO，工具调用/生成过程默认可见
- 部署插曲：rsync --delete 误删 Dockerfile/deploy 致 nginx 挂载失败，从备份补回恢复，已记入发版铁律（传输集必须含 compose 根文件）

### 4. 模型成本治理（11:00–13:23）
- 账户审计：主生成=deepseek-v4-pro（DeepSeek 官方，余额 -0.28 元停服）、OCR=kimi-k3（Moonshot，正常）；查明消耗快的根因=工具循环每轮全量重发历史，单章累计百万级 token
- 已上线降本：Kimi 默认模型降档为 moonshot-v1-32k-vision-preview（仅 OCR 读图，读图能力实测正常），重建容器生效
- 已备好待部署：单文档返回上限 20000→10000（预计砍半累计 token），等 DeepSeek 充值到账即部署；轮询 15 分钟未到账，待用户确认充值去向

### 5. 重跑失败诊断（10:14–11:10）
- 新代码下两轮第一章重跑均失败但均不写文件（报错路径无副作用）：第一轮 25 分钟触工具轮数上限、强制输出空；第二轮开 INFO 后现场可见 35 次定点工具调用（权属证/估价报告/天眼查股东）行为正常，第 14 轮起 DeepSeek 402 欠费终止——确认堵点是账户余额而非代码

## 二、做得好的地方

- **事故处置零丢失**：备份→清任务→恢复→清理→验证五步闭环，所有中间态快照留隔离目录可回溯，用户数据未受二次伤害。
- **现场证据驱动**：开 INFO 日志后一轮即区分"模型行为正常、账户欠费"与"代码缺陷"，避免盲目改代码。
- **铁律沉淀**：欠费事故后先重启清任务、重跑前必备份 ch{n}.json；发版传输集必须覆盖 compose 根文件——两条均已入记忆库。
- **降本不降链路**：Kimi 降档前先实测读图能力，保留一键回切 kimi-k3 的退路。

## 三、未提交/进行中事项

- `backend/routers/skills.py`、`backend/services/skill_runner.py`、`frontend/js/app.js`（09:55–10:01）：串章过滤+小标题强约束+前端竞态修复，**已发版生产**，待提交
- `backend/main.py`（10:45）：INFO 日志，**已发版生产**，待提交
- `backend/services/materials_client.py`（13:23）：_MAX_TEXT 20000→10000 降本，**未部署**，等 DeepSeek 到账

## 四、待干清单

1. DeepSeek 充值到账后部署 _MAX_TEXT 降本改动（用户已确认：到账即部署，但暂不重跑第一章）
2. 提交已发版生产的 4 处修复代码
3. 用户通知后按"先备份、干净触发"流程重跑第一章；第三章按需首次生成
4. 观察 Kimi 降档后扫描件 OCR 质量，劣则回切 kimi-k3
5. 与用户核对 DeepSeek 充值账号（key 前缀 sk-d42d736e）是否充错账户

## 五、时间线速览

| 时间段 | 主题 |
|--------|------|
| 09:10–09:44 | 欠费复活任务写坏 ch1/ch2/ch3（事故发生） |
| 09:12 | reading-ch2 EBITDA 取数强化（提交） |
| 09:46–10:00 | 重启清任务 + 备份恢复 ch1/ch2 + 隔离 ch3 + 快照保护 |
| 09:55–10:10 | 串章根因修复全量发版（含部署插曲处置）+ 数据清理 |
| 10:14–11:10 | 两轮重跑诊断：工具上限/402 欠费，确认堵点 |
| 10:45 | INFO 日志发版 |
| 11:00–12:00 | 账户审计 + Kimi 降档降本上线 |
| 13:23 | _MAX_TEXT 降本备好，轮询余额未到账 |

**今日合计：1 次提交 + 5 处未提交改动（4 处已上生产）；完成 1 起生产事故全链路处置（零丢失）、1 次根因修复发版、1 轮模型成本治理。**
