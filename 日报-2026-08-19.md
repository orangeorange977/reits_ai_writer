# 工作日报 · 2026年8月19日（周三）

> 数据来源：当日无新提交，全部工作在未提交改动中（23 文件，+1111/-181 行），文件时间戳 09:36–17:36 全天连续；两份官方参考 PDF 入库（摘要和释义表 09:36、第一章 16:11）；MiniMax 生产部署经部署记录交叉验证。

## 一、今日已完成事项（按主题）

### 1. 卷首"摘要表和释义"skill 新建与导出注入闭环（09:36–14:54）
- 官方《摘要和释义表.pdf》参考资料入库（09:36），对照新建 reading/summary.md skill（双副本），定义摘要表 22 个固定行项与释义三列表式结构（09:51）
- 技能管理列表新增"卷首 · 摘要表和释义"条目，与各章写作要求同级可编辑（packs.py + skills_admin.js，10:09–10:11）；planning.md 登记该 skill（11:14）
- 摘要页默认骨架：未保存或保存为空时自动回填 22 行固定行项（值留空），打开页面即见基本表格（summary_service.py，14:32）
- 修复导出缺释义根因（官方模板无该节骨架）：web_render.py 新增 fill_front_matter 自动填充摘要表并插入完整释义节，另加 ensure_page_breaks 分页控制与表格首列纵向合并规范化（14:40，+252 行）
- 摘要 AI 生成链路打通：后端 run_summary 后台任务 + /skills/summary/run、/skills/summary/status 接口（任务键复用章节号 0）；前端摘要页新增"AI 生成/Kimi 助手"按钮 + 轮询（skills.py、app.js、api.js，14:41–14:54）
- 实测验证：项目 1 数据 22 行摘要填值 + 54 行释义表成功注入导出文档

### 2. MiniMax 第三厂商接入与 OCR 厂商切换（13:38–17:36，当日生产部署）
- 先验证后接入：确认 key 在国内端点 api.minimaxi.com 有效，实测 MiniMax-M3 支持 clean JSON（thinking disabled）、图像 OCR、函数调用三项能力后才动路由
- config.py 新增 MINIMAX_API_KEY/BASE_URL/MODEL/VISION_MODEL 四项配置（13:38）；.env、model_setting.json 切换 MiniMax-M3（13:45）
- kimi_client.py 三厂商路由重构：_is_minimax 判断 + 所有 MiniMax 调用带 thinking={"type":"disabled"} 避免 JSON 解析失败；ocr_images 改为自动跟随当前主模型厂商（17:36）
- skill_runner.py 增加 _supports_web_search 模型能力门控，不支持联网搜索的厂商自动跳过该工具（13:44）
- 生产部署完成：服务器 .env 追加 MiniMax 配置，四文件 docker cp 热部署 + force-recreate 重建容器后终验全链路（模型列表、get_selected_model、chat clean JSON、OCR 均通过）

### 3. 评测打分两项体验增强（15:18–15:20）
- 打分新鲜度机制：打分时记录生成稿 mtime 指纹，章节重新生成后旧分数自动标记"过期"，按钮变"重新打分（生成稿已更新）"+ 打分卡顶部橙色过期提示，避免旧稿分数误导；历史无指纹记录用打分时间兜底判断
- 删除打分历史：新增 DELETE /eval/score/{n} 接口 + 前端删除按钮，二次确认后仅删打分记录（不影响标准答案与对比结果），评分进行中禁止删除

### 4. 第五章 skill 防编造红线修订（15:47）
- 手续表新增防编造红线：签发时间/机构/文号一律从 13 号文件夹原件逐字摘录，严禁按格式推断生成文号，无法辨识的填【注：待核对原件】
- 土地/房屋权属两表新增证载信息提取红线：按证逐行登记、不得漏行合并，无法提取填【注：待核实】严禁留空编造
- 可转让性汇总表新增填列红线：严禁空表/整行为空，按摘要表子项目逐个填列
- PPP 表 21 结构硬性化：非 PPP 项目正文只输出"不涉及。"严禁输出 15 行空表；属 PPP 的严格 15 行×2 列基线行序，严禁输出单列清单

### 5. 参考资料入库（16:11）
- 官方《第一章.pdf》就位，为第一/二章 skill 逐章对照修订做准备（次日展开）

## 二、进行中事项（未提交）

- 当日全部改动共 23 文件（+1111/-181）尚未提交：摘要释义 skill 闭环、MiniMax 接入、评测增强、ch5 红线修订，待整体回归后提交入库
- 生产环境当前为 docker cp 热部署状态，正式发版待与本地提交一并走发版流程

## 三、做得好的地方

- **摘要释义从根因到闭环一天打通**：定位"导出缺释义"根因是模板无骨架而非生成缺失，fill_front_matter 程序化注入 + skill 化管理 + AI 生成链路一次配齐，并有实测注入证据（22 行摘要 + 54 行释义）。
- **MiniMax 接入先验证后改造**：key 有效性、clean JSON、OCR、工具调用四项能力逐一实测通过才动路由代码，且 OCR 自动跟随主厂商的设计消除了双处切换的配置漂移。
- **打分新鲜度用文件指纹而非人工提醒**：gen_mtime 机制让"旧稿分数误导"问题从流程约束变成系统保证，历史记录还有时间兜底兼容。

## 四、待干清单

1. 当日 23 文件改动整体回归验证后提交入库（摘要释义、MiniMax、评测增强、ch5 红线）
2. 第一/二章 skill 对照官方 PDF 逐章修订（第一章.pdf 已就位）
3. 按发版流程正式发版生产（当前为 docker cp 热部署，force-recreate 会抹掉热部署文件，须防丢失）
4. MiniMax 生产运行观察：各章生成、OCR 识别、AI 打分全链路在真实项目上验证
5. 官方参考 PDF（摘要释义 + 七章）与日报/周报文件入库留档决定（当前均为未跟踪文件）

## 五、时间线速览

| 时间段 | 主题 |
|--------|------|
| 09:36–11:14 | 摘要和释义表 skill 新建 + 技能管理接入 + 22 行默认骨架 |
| 13:38–13:45 | MiniMax 配置接入，模型切换 MiniMax-M3 |
| 14:32–14:54 | fill_front_matter 导出注入 + 摘要 AI 生成/Kimi 助手链路 |
| 15:01–15:20 | 评测增强：打分新鲜度过期标记 + 删除打分历史 |
| 15:47 | ch5 skill 四处防编造/填列红线修订 |
| 16:11 | 第一章官方 PDF 参考资料入库 |
| 17:36 | kimi_client 三厂商路由收尾（OCR 跟随主厂商），生产部署终验 |

**今日合计：0 次提交（全部工作在未提交改动中，23 文件 +1111/-181）；完成摘要释义 skill 闭环、MiniMax 接入与生产部署、评测两项增强、ch5 红线修订；引入 2 份官方参考 PDF。**
