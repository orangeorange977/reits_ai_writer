# 工作日报 · 2026年8月19日（周三）

> 依据：当日 23 文件改动（+1111/-181 行，已随次日 afc51e9 提交入库）、文件时间戳 09:36–17:36，生产部署经部署记录交叉验证。

## 今日工作汇报

今天的工作围绕三条主线展开：补齐导出文档的卷首内容、接入并实测第三家模型厂商、以及针对生成质量问题的评测与规则加固。

**第一，打通了卷首"摘要表和释义"的完整链路，解决了导出文档一直缺释义节的问题。**排查确认根因不在 AI 生成侧，而是官方 2024 版模板本身就没有释义节骨架、摘要表也只是空壳，光靠模型生成填不进正确位置。因此对照官方格式文本新建了"摘要表和释义"写作技能（22 个固定行项 + 三列释义表），接入技能管理页与各章写作要求同级可编辑；数据层做了默认骨架兜底，用户打开摘要页即见完整表格结构；导出侧新增程序化注入引擎，自动把已保存的摘要数据填入模板表格、把释义节整节插入正文之前，并顺带解决了摘要表、释义、各章标题的强制分页排版。同时打通了摘要的 AI 一键生成和 Kimi 助手辅助录入。用真实项目数据实测，22 行摘要填值和 54 行释义表均成功注入导出文档，该问题闭环。

**第二，完成 MiniMax 作为第三家模型厂商的接入与生产部署，并在当天的真实使用中得到了重要结论。**接入采取"先验证后改造"的路径：先确认密钥在国内端点有效，再逐项实测 MiniMax-M3 的结构化输出、图片识别、工具调用三项能力，全部通过后才改造后端路由——包括三厂商统一路由判断、针对 M3 思维链输出会破坏 JSON 解析的参数适配、文档识别自动跟随所选主模型厂商（避免切换模型还要单独切识别引擎的配置漂移）、以及联网搜索的模型能力门控。当天完成生产环境配置更新与容器重建，全链路终验通过。但随后用第五章做真实生成实测时暴露了质量问题：MiniMax-M3 出现大面积空表和手续文号编造，评分从 deepseek 时代的 63–65 分跌到 27 分，据此当天即切回 deepseek-v4-pro 作为主力模型（当天因 deepseek 账户余额不足曾临时回退 MiniMax，次日充值后恢复），MiniMax 保留为已接入的兜底厂商。这次实测虽然否定了直接切换，但暴露的空表、编造、取数不稳等问题为后续的生成可靠性治理指明了具体方向。

**第三，评测功能补上了两块体验短板，第五章写作规则针对实测暴露的问题做了加固。**评测方面，AI 打分现在会记录打分时所依据生成稿的文件指纹，章节一旦重新生成，旧分数自动标记"过期"并提示重新打分，杜绝旧稿分数误导判断；同时支持删除打分历史，带二次确认且不影响标准答案和对比结果。写作规则方面，针对当天第五章实测暴露的编造和空表问题，在技能文件中加了四处硬约束：手续文号必须从原件逐字摘录、严禁按格式套路自行生成；权属证载信息必须按证逐行登记；可转让性表严禁空表交卷；PPP 合规表严格 15 行两列结构、非 PPP 项目只写"不涉及"不出空表。

此外，官方《摘要和释义表》《第一章》两份格式 PDF 已入库，后者为明天开始的第一、二章写作规则逐章修订做准备。

## 做得好的地方

今天有两点值得肯定：一是缺释义的问题没有停留在改提示词碰运气，而是追到"模板本身无骨架"这个真正的根因，用程序化注入一次性解决，并配了失败降级保护；二是 MiniMax 的接入与退出都走了实证路径——接入前逐项验证能力才动代码，接入后用真实章节实测发现质量不达标，当天果断切回 deepseek-v4-pro，没让问题流入正式使用，且实测暴露的问题直接转化为了后续治理清单。

## 下一步计划

今日 23 个文件的改动待整体回归后提交入库；第一、二章写作规则将对照官方 PDF 逐章修订；生产环境目前处于热部署状态，需与本地提交一并走正式发版流程；MiniMax 暴露的生成可靠性问题（空表、编造、取数不稳）将作为明天的治理重点逐项根治。

## 时间线速览

| 时间段 | 主题 |
|--------|------|
| 09:36–14:54 | 摘要释义技能新建、技能管理接入、导出注入引擎与 AI 生成链路 |
| 13:38–17:36 | MiniMax 接入、三厂商路由改造、生产部署与全链路终验 |
| 15:18–15:20 | 评测增强：打分过期标记 + 删除打分历史 |
| 15:47 | 第五章四处防编造/防空表硬约束 |
| 16:11 | 官方《第一章.pdf》入库备用 |

**今日小结：摘要释义链路闭环（实测注入成功）、MiniMax 完成接入但实测后切回 deepseek-v4-pro、评测两项增强、第五章规则加固；共改动 23 文件 +1111/-181 行。**
