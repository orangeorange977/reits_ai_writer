# REIT-AI-System 修改步骤（详细版）

> 依据：《问题核实报告.md》（2026-08-05）
> 原则：每个阶段独立完成、独立验收，改完任何一步系统都可正常运行；先修 bug，再做架构，最后上线。
> 预估：阶段一 1~2 天；阶段二 3~7 天；阶段三 1~2 周。

---

# 阶段一：实证 bug 修复 + 工程化打底 ✅ 已完成（2026-08-05，tag: v1.1-bugfix）

> 目标：消除已证实的坏功能，建立版本管理，清掉假数据。不动架构，风险最低。

## 步骤 1.1 初始化 git 仓库 ✅

**操作：**
1. 在 `REIT-AI-System/REIT-AI-System/` 下执行 `git init`。
2. 编写 `.gitignore`（覆盖现有简版）：
   ```gitignore
   __pycache__/
   *.pyc
   .venv/
   .env                # 含真实 Kimi/天眼查密钥，绝不可入库
   workspace/          # 运行期数据库和输出
   server_*.log
   .deps_installed
   *.docx
   !backend/templates/official/*.docx   # 官方模板例外保留
   ```
3. 首次提交全部源码。

**验收：** `git status` 干净；`.env`、`workspace/`、`__pycache__/` 不出现在 `git ls-files` 中。

**注意：** 外层 `skills/` 目录在仓库之外，阶段二会把它收编为模板包，届时再入库。

## 步骤 1.2 清理运行垃圾与重复文件 ✅

**操作：**
1. 删除全部 `__pycache__/` 目录、`server_out.log`、`server_err.log`（已入 .gitignore，以后不会再混入）。
2. 对比 `run.bat` 与 `启动网站.bat`：保留 `启动网站.bat`（功能全：建 venv、装依赖、开浏览器），确认 `run.bat` 无独有逻辑后删除。
3. `.deps_installed` 是 bat 的依赖标记，保留机制但不入库。

**验收：** 项目根目录只剩源码、配置、启动脚本和文档。

## 步骤 1.3 修复 4 处前后端接口不匹配 ✅

**原则：以后端路由为准改前端**（后端 managers 逻辑是完整的）。

**操作（全部在 `frontend/js/enhancements.js`）：**

| # | 位置 | 现状 | 改为 |
|---|---|---|---|
| 1 | 第 32/35 行 | `/projects/${projectId}/financial` | `/projects/${projectId}/financial-data` |
| 2 | 第 43 行 | `API.put(.../inapplicable, { section_ids: sectionIds, reason })` | `{ sections: sectionIds, reason }` |
| 3 | 第 51 行 | `API.put(.../query-dates, { dates })` | 平铺为 `{ base_date, query_point, extra }`（按 `enhancements.py` 第 50~54 行的 `QueryDatesUpdateRequest` 模型） |
| 4 | 第 16/19 行 | `addGlossaryEntry`(POST /glossary)、`deleteGlossaryEntry`(DELETE /glossary/{id})，后端无此路由 | 先全局检索调用点：若无调用直接删除这两个函数；若有调用，改为在前端合并 entries 后调已有的 `PUT /glossary` 整体保存 |

**验收：** 启动服务，打开"发改委材料生成 → 高级选项"，6 个 tab 逐个做"读取 + 修改 + 保存"，浏览器 Network 面板无任何 4xx/5xx。

## 步骤 1.4 删除前端硬编码假数据 ✅

**操作（`frontend/index.html`）：**
1. 系统概览页：清空 `#projectTable tbody`、`#recentTimeline`、`overviewStats` 里的万国数据假数据，替换为"加载中…/暂无数据"空态；模板版本表如不接 API 就删除该卡片。
2. 发改委材料生成页：项目信息栏（`project-header-bar`）改为 JS 按当前项目渲染；`chapterFields` 假字段列表清空。
3. 文档管理页：`#docTable tbody`、`#docVersionTimeline` 假数据清空，改由 `loadDocuments()` 真实渲染（没有版本历史 API 的卡片先隐藏）。
4. 章节编辑页（`page-chapter-edit`）：整页是旧视图假数据，确认无入口后**整页删除**（新流程已走 `renderChapterEditor`）。
5. 项目详情弹窗（`modal-project-info`）：假数据清空，改由 JS 填真实项目信息，或暂时删除入口按钮。

**验收：** 断开后端直接开页面，任何位置都看不到"万国数据"字样；连上后端后各区域显示真实数据。

## 步骤 1.5 补开发者 README.md ✅

**内容：** 架构图（两条管线现状 + 目标方向）、目录结构说明、启动方式（bat / `python run_server.py`）、API 文档入口（`/docs`）、外层 `skills/` 的作用与写法。
**验收：** 新同事只看 README 能把服务跑起来。

## 阶段一总验收

- [x] 6 个增强 tab 读写全部正常（curl 实测全部读写往返 200；浏览器人工验收待用户确认）
- [x] 全站无假数据（`grep 万国|奥飞|润泽 frontend/` 零命中）
- [x] git 首次提交完成，工作区干净（4 次提交，tag: v1.1-bugfix）
- [ ] 逐章生成 → 编辑 → 保存 → 预览 → 下载 Word 主流程不回归（需浏览器人工确认；本次未改新管线代码，回归风险低）

## 阶段一实施记录（2026-08-05）

**提交历史：**
```
4e167c6 (tag: v1.1-bugfix) 文档：补开发者 README
9ffcaac 清理：删除前端硬编码假数据（整页删旧章节编辑视图，项目信息/表格改JS真实渲染）
de5709d 修复：增强功能4处前后端接口不匹配（路径/请求体/响应解包对齐后端）
208546d 初始化：导入现有源码（清理运行垃圾与重复启动脚本后）
```

**与原计划的差异及额外修复：**

1. 步骤 1.1/1.2 合并执行：先清理垃圾再首次提交，避免垃圾文件进入 git 历史。
2. 步骤 1.3 核查中发现计划外的 3 处接口问题并一并修复（均在 `frontend/js/enhancements.js`）：
   - 6 个 tab 的 GET 都没有解后端 `{success, data}` 包裹，读取静默失败、全靠前端假数据兜底 → 新增 `_unwrapEnhanceResp()` 统一解包；
   - 附件保存请求体字段与后端 `AttachmentItem` 模型不符（前端 `name/type` vs 后端 `title/filename`，会 422）→ API 层双向转换；
   - “不涉及”后端只存已勾选模块 ID 列表 → 读取时改为默认树结构 + 按 ID 回填勾选状态。
3. 基准日映射规则：评估基准日→`base_date`，申报日期→`query_point`，其余 4 个日期收入 `extra` 原样保存，读取时反向还原，保证往返一致。
4. 步骤 1.4 除 index.html 外，同步改了 `app.js`（新增 `_projectsCache`/`updateProjectHeaderBar()` 真实渲染项目信息栏、空项目空态）和 `enhancements.js` 兜底释义表（去除具体机构名）；`page-chapter-edit` 整页删除后，相关 JS 函数（`previewChapter`/`editField` 等）暂保留，因旧管线渲染代码仍引用，随阶段二 2.6 一并清除。
5. curl 验证时向本地 `workspace/` 库写入了少量测试 metadata（财务/基准日/附件等），属运行期数据，不入库，可忽略或在页面里改回。

---

# 阶段二：三层分离（引擎 / 模板包 / 项目数据） ✅ 已完成（2026-08-05，tag: v1.2-packs）

> 目标：引擎不认识"REITs"，REITs 发改委 2024 版成为第一个可插拔模板包；换项目/换材料类型不改代码。

## 步骤 2.1 定义模板包规范 ✅

**新建目录结构：**
```
templates-packs/
└── reits-ndrc-2024/
    ├── manifest.json        # {id, name, version, description, chapter_count}
    ├── template.docx        # 官方格式文本（从 backend/templates/official/ 迁入）
    ├── chapters.json        # 章节结构（替代 skill_runner.CHAPTERS 硬编码）
    ├── planning.md          # 全局总纲（从 skills/ 迁入）
    ├── reading/
    │   ├── ch1.md … ch7.md  # 各章写作要求（从 skills/reits-reading-chN/SKILL.md 迁入并清洗）
    ├── writing/
    │   ├── SKILL.md         # 排版要求（从 skills/reits-writing/ 迁入）
    │   └── web_render.py    # 渲染引擎脚本（随包走，保留热重载机制）
    └── diagrams/            # drawio 模板（从 skills/reits-diagrams/templates 迁入）
```

`chapters.json` 内容即现在 `skill_runner.py` 第 22~30 行 `CHAPTERS` 的 JSON 化：
```json
{
  "chapters": [
    {"n": 1, "title": "一、项目基本情况", "next": "二、参与主体情况", "reading": "reading/ch1.md"},
    {"n": 7, "title": "七、募集资金用途情况", "next": null, "reading": "reading/ch7.md"}
  ]
}
```

## 步骤 2.2 引擎改为模板包驱动 ✅

**操作（`backend/services/skill_runner.py` 为主）：**
1. 新增 `backend/services/pack_service.py`：扫描 `templates-packs/`、加载 manifest/chapters.json、提供 `get_pack(pack_id)`。
2. `skill_runner.py` 的模块级常量 `CHAPTERS`、`_MODEL_SETTING_PATH`、`WRITE_CONFIG_PATH` 等改为按 pack 动态解析；所有 `SKILLS_DIR / CHAPTERS[n]["dir"]` 路径改为 `pack.dir / pack.reading_path(n)`。
3. `backend/config.py`：`SKILLS_DIR` 改为 `PACKS_DIR`（默认 `templates-packs/`）；删除 `NDRC_MATERIALS_PATH`、`CSRC_MATERIALS_PATH` 等业务路径。
4. `routers/skills.py` 的 `_load_web_render`、`_DIAGRAM_TPL_DIR` 改为从当前项目的 pack 加载（保留 `importlib.reload` 热改机制）。

**验收：** 只保留 reits-ndrc-2024 一个包时，全部功能与改造前一致（对照测试：同一项目重新生成第 1 章，产物一致）。

## 步骤 2.3 清洗写作规则中的项目特定内容 ✅

**操作（迁入模板包后的 reading/*.md）：**
1. `ch2.md` 第 215~221 行：删除奥飞项目四段承诺函原文（真实日期、真实机构名），改写为通用指引——"用 list_materials/read_document 阅读对应承诺函，据实摘录落款日期、机构名与承诺措辞"。
2. `ch3.md` 第 33 行：示例行"奥飞数据"改为占位 `【原始权益人简称】`。
3. 全包检索 `奥飞|润泽|万国`，清零为止。
4. 项目数据文件（`ch1.json`、`ch2.json`、`summary_saved.json`、`ch1_content_*.json`）**不进模板包**，按步骤 2.5 归入项目数据目录。

**验收：** `grep -rn "奥飞\|润泽\|万国" templates-packs/` 无结果。

## 步骤 2.1~2.3 实施记录（2026-08-05）

**已建成的模板包**（`templates-packs/reits-ndrc-2024/`，全部入库）：manifest.json（新增 `material_label` 字段供引擎拼提示词）、template.docx、chapters.json、planning.md（本就是通用总纲、无项目词）、reading/ch1~ch7.md、writing/SKILL.md + web_render.py、diagrams/*.drawio。

**引擎改造（2.2）：**
- 新增 `backend/services/pack_service.py`：扫包/读 manifest/解析 chapters.json/包内资源定位，`list_packs()`、`get_pack()`、`reading_path()`、`material_label()` 等；
- `skill_runner.py`：`CHAPTERS` 改为启动时从默认包 chapters.json 加载（删硬编码）；`run_chapter` 的 planning/写作要求改从包内读；三处重复的 web_render 加载逻辑合并为 `load_web_render()`（保留 `importlib.reload` 热重载）；
- `config.py`：`SKILLS_DIR`/`PLANNING_MD_PATH` 删除，新增 `PACKS_DIR`、`PROJECTS_DIR`、`DEFAULT_PROJECT_ID`；`NDRC/CSRC_MATERIALS_PATH` 确认无引用后删除；
- `routers/skills.py`：web_render/画图模板改从默认包加载；模板路径回退链改为“用户路径 → 包内 template.docx → config → 源码目录”；
- `web_render.py` 的 write_config 定位改为优先读引擎注入的环境变量 `WRITE_CONFIG_PATH`（运行期数据在 workspace，不进包）。

**项目数据布局（为 2.4 铺路，过渡期用 default 项目）：**
- 章节 JSON/Word 输出：`workspace/projects/default/ch{n}.json`、`output/ch{n}_output.docx`；
- 摘要表：`workspace/projects/default/summary_saved.json`；
- 全局设置：`workspace/model_setting.json`、`workspace/write_config.json`；
- 旧 `skills/` 目录的运行期数据已拷贝迁入（原目录在仓库外，不再被引擎引用，可人工删除）。

**清洗（2.3）：**
- 除计划内 2 处（ch2 承诺函四段原文→通用据实摘录指引、ch3 示例行→【原始权益人简称】）外，扩大检索又清了 8 处计划未列的真实机构/主体名（南方基金/南方资本/华泰联合/固安聚龙/昊盈科技/注册地址/股权结构等）→全部改占位符；`奥飞|润泽|万国|南方基金|南方资本|华泰|国浩|司农|国友大正|固安|昊盈|深圳` 全包零命中；
- 引擎代码去业务词：`REITs发改委申报材料` 提示词改读 manifest.material_label，main.py 描述通用化；新管线（services/skills/enhancements/folders/projects/main/config/database）已零业务词；旧管线（generators/parsers/mappings/managers）残留词随 2.6 处置。

**回归验证：** 启动无错；summary/models/diagram-templates/章节内容（含模板小标题与骨架）/预览（used_template=True，产物写入新项目目录）/下载全部 200；浏览器控制台零错误、选章编辑器正常渲染。

**遗留：** 外层 `skills/` 目录未删（仓库外，引擎已不引用）；阶段二总验收需待 2.4~2.6 完成后统一勾选。

## 步骤 2.4 项目数据按项目隔离 ✅

**操作：**
1. 新存储布局：`workspace/projects/<project_id>/` 下存 `summary_saved.json`、`ch{n}.json`、`output/*.docx`。
2. `skill_runner.py`：`chapter_json_path(n)`、`chapter_docx_path(n)`、`_save_json` 等全部加 `project_id` 参数；`summary_service.py` 的 `SAVED_SUMMARY_PATH` 改为按项目解析。
3. `routers/skills.py`：`/skills/chapter/{n}/...` 系列路由加 `project_id`（路径或查询参数），`_jobs`/`_PREVIEW_CACHE` 的 key 改为 `(project_id, n)`。
4. 写一次性迁移脚本：把现有 `skills/summary_saved.json`、`ch{n}.json` 归入"默认项目"目录。

**验收：** 建两个项目交替生成/编辑，互不串数据；旧数据迁移后可正常打开。

## 步骤 2.4 实施记录（2026-08-05）

**实施内容：**
1. **统一净化**：`config.py` 新增 `safe_project_id()`（取 `Path.name` + 拦截 `''`/`.`/`..` 落回 default），skill_runner/summary_service 共用，防目录穿越。
2. **skill_runner.py**：`_load_json`/`_save_json`/`get_chapter_content`/`get_chapter_structured`/`save_chapter_content`/`run_chapter`/`_format_saved_summary` 全部加 `project_id` 参数并贯通到落盘路径。
3. **summary_service.py**：`SAVED_SUMMARY_PATH` 常量改为 `saved_summary_path(project_id)` 函数，读写均按项目解析。
4. **routers/skills.py**：章节 run/status/content/save/preview/download 及 summary 读写 7 个路由加 `project_id` 查询参数；`_jobs`/`_tasks`/`_PREVIEW_CACHE` 的 key 改为 `(pid, n)`，预览签名也含项目维度。
5. **frontend/js/api.js**：新增 `_currentProjectId()`（读全局 `currentProjectId`，未选项目时空串落回 default），上述接口的请求全部自动携带 `project_id`。
6. **迁移脚本** `scripts/migrate_project_data_2_4.py`：把过渡期 `workspace/projects/default/` 的 7 个文件（含 output）幂等分发到 DB 里每个已有项目目录（现为 projects/1/），default 保留作兜底。

**回归验证（全过）：**
- 隔离性：项目 1 摘要 22/53/15 条、ch1 source=ready；测试项目（999/2）摘要全空、ch1 为 template 骨架；给测试项目存数据后回读正确，且项目 1 不受影响；
- 穿越防护：`project_id=..` 落回 default（读到 22 条），workspace/ 下无逃逸产物；
- 预览/保存/状态：项目 1 preview used_template=True（html 9126 字符）、ch2 save 落盘到对应项目目录、status idle；
- 浏览器：控制台零错误，网络请求全部带 `project_id=1`，编辑器渲染真实数据（3 个小标题、已生成 badge）。

**遗留：** 前端章节步骤条标题仍用前端写死的七章名（随 2.5 改为从包 chapters.json 渲染）；旧管线路由仍挂载（随 2.6 处置）。

## 步骤 2.5 模板包注册与项目绑定 ✅

**操作：**
1. `projects` 表加列 `pack_id`（Alembic 迁移或启动时 `ALTER TABLE` 判断加列）。
2. 新路由 `GET /api/packs`：列出可用模板包（id、名称、版本）。
3. 前端：新建项目弹窗增加"材料模板"下拉（数据来自 `/api/packs`）；章节步骤条标题从 pack 的 chapters.json 渲染，替代前端写死的七章名。
4. `frontend/index.html` 标题"发改委材料生成"等文案改为按 pack 名称动态显示。

**验收：** 复制 reits-ndrc-2024 改名为测试包并微调标题，前端能选、能按新结构生成。

## 步骤 2.5 实施记录（2026-08-05）

**实施内容：**
1. **DB**：`projects` 表加 `pack_id` 列（建表即含；旧库启动时 `ALTER TABLE` 判断补列，失败静默），并把存量项目回填为默认包；新增 `get_project_pack_id()`。
2. **新路由** `routers/packs.py`：`GET /api/packs`（含 `default_id`）与 `GET /api/packs/{id}`（manifest + 章节列表），包不存在返回 404。
3. **项目路由**：创建项目可传 `pack_id`（校验包存在，不传绑默认包），项目列表/详情返回 `pack_id`。
4. **引擎 pack_id 贯通**：`skill_runner` 新增 `chapters_for(pack_id)`，`save_chapter_content`/`run_chapter`/`chapter_subtitles`/`chapter_tables`/`chapter_table_start`/`ensure_write_config`/`_writing_skill_md` 等全部按包解析；`routers/skills.py` 新增 `_project_pack_id()`（查 DB），章节 run/status/content/save/preview/download 全链路透传，预览签名含包维度（换绑包后预览自动重算）。
5. **前端**：`api.js` 新增 `getPacks`/`getPackDetail`，`createProject` 带 `pack_id`；`app.js` 新增全局 `PACK_INFO`/`PACK_CHAPTERS` 与 `loadProjectPack()`（按当前项目绑包拉取），步骤条改从包章节结构渲染（写死的七章名仅作兜底），`_chapterTitle(n)` 替换 3 处硬编码标题；nav 文案与页面标题按包名动态显示；新建项目弹窗增加"材料模板"下拉（默认选中 `default_id`）。
6. **测试包** `templates-packs/reits-ndrc-test/`：复制自 reits-ndrc-2024，六章结构（删第七章），第六章标题改"运营管理安排（测试修订）"，template.docx 的 Heading1 同步改写。
7. **修复**：`chapter_docx_path()` 自动创建 output 目录（新项目首次预览因目录缺失报 FileNotFoundError 的验收 bug）；`_project_pack_id` 返回注解用 `Optional[str]`（该 Python 版本不支持运行时 `str | None`）。

**验收（全过）：**
- `GET /api/packs` 返回两包（default=reits-ndrc-2024）；测试包详情 6 章；
- 绑测试包的项目：ch6 内容骨架来自测试包模板（source=template）、ch7 返回 404，而绑默认包的项目 ch7 正常——结构随包走；
- ch6 save → preview：used_template=True，预览 HTML 与产出 docx 的 Heading1 均为"六、运营管理安排（测试修订）"，正文写入新标题下；
- 浏览器：新建项目弹窗下拉显示两包且默认选中 default_id；通过 UI 链路新建项目绑测试包后，nav/页面标题显示包名、步骤条为固定"摘要表和释义"+六章（含修订标题）；切回项目 1 恢复七章与 2024 包名；控制台零错误。

**清理：** 验收用测试项目（ID 2/3）已从 DB 与 workspace 删除；测试包 reits-ndrc-test 保留作为可插拔包的活证据。

**遗留：** 删除项目路由不清理 `workspace/projects/<id>/` 数据目录（随 2.6 或后续补）；旧管线路由仍挂载（随 2.6 处置）。

## 步骤 2.6 旧管线处置（先做 2.2~2.5 验证新管线完整后再做） ✅

**推荐：整体删除旧管线**，因为它与新管线功能重叠且无人维护：
- 删除 `routers/projects.py`（项目 CRUD 需先在新管线补等价路由）、`chapters.py`、`generate.py`、`generators/`、`mappings/`、`parsers/` 中仅旧管线使用的部分。
- **例外保留**：`managers/`（增强功能五件套）——它被 `enhancements.py` 使用，先保留；后续再把增强功能接到新管线的生成后钩子（现有 `apply_all_enhancements` 思路可沿用）。
- 前端同步删除旧视图残留代码与失效按钮。

**保守替代方案：** 保留但全部打 `deprecated` 标记并从导航隐藏入口。

**验收：** 主流程（建项目→逐章生成→编辑→预览→下载）在删除旧管线后完整可用。

## 步骤 2.6 实施记录（2026-08-05）

**删除（整体删旧管线，采纳推荐方案）：**
1. **后端**：`routers/chapters.py`、`routers/generate.py`、`generators/` 整目录、`parsers/` 的 docx/pdf/xlsx 三个结构化解析器（`parsers/utils.py` 保留，folders 路由在用）、`models/` 整目录（零引用）；`routers/projects.py` 去掉 NDRCGenerator 依赖并删掉无人调用的 `/projects/{id}/scan`；`main.py`/`routers/__init__.py` 同步去掉旧路由注册。
2. **例外保留**：`managers/`（增强五件套）+ `routers/enhancements.py` + `mappings/`（被前两者与 db.py 预置导入依赖）+ `routers/folders.py`（本机路径选择器，阶段三 3.3/3.4 再处置）。
3. **新管线补等价路由**：`GET /api/skills/documents?project_id=` 列出项目已生成的各章 Word（文档管理页数据源，替代旧 `/projects/{id}/documents`）。
4. **前端**：`api.js` 删 8 个旧接口封装（chapters/generate/download/documents 系列），`getDocuments` 改指 `/skills/documents`；`app.js` 删 loadChapterDetail/previewChapter/editField/generateAll/generateSelected/startProgressPolling/saveChapterEdit/resetChapterEdit/downloadDoc 等旧视图函数与 WIRED_CHAPTERS 分流（selectChapter 全走新编辑器）；底部旧生成按钮移除；整体进度条改按"有内容章节占比"计算（`updateChapterProgress`）；文档管理页表格改渲染新管线文档列表（章标题+文件名+大小+下载）；`components.js` 从 517 行精简到约 100 行（只留 renderStatCards/renderProjectTable/renderStepper/toggleSection）。

**验收（全过）：**
- 旧路由已下线：`/api/projects/1/chapters` 落入 SPA 通配兑底（非旧路由响应）、generate/scan 返回 405；后端对已删模块零引用；
- 主流程：内容编辑→save→preview（used_template=True、编辑标记出现在预览）→还原；文档管理页列出项目 1 的 ch1/ch2 两份产物并可下载；控制台零错误；
- 阶段二总验收四项：引擎代码无业务词（残留仅在保留模块 docstring）、模板包自包含、项目隔离（项目 1 摘要 22 行 vs 项目 999 全空）、测试包免改 Python 代码即可选用。

**遗留：** 删除项目不清理 `workspace/projects/<id>/` 数据目录；本机路径浏览（browse-any）与无鉴权待阶段三处置。

## 阶段二总验收 ✅

- [x] `grep -rn "REITs\|发改委" backend/` 只剩注释/文档，引擎代码无业务词
- [x] 模板包目录自包含（换一台机器只拷 templates-packs + 代码即可用）
- [x] 两个项目数据互不影响
- [x] 新增一个测试模板包无需改任何 Python 代码即可选用

## 阶段二全量复查 ✅（2026-08-05，提交 2c1b656，tag v1.2-packs 已移至此）

对 2.1~2.6 全部 60 个变更文件逐项重查，发现并修复 3 个问题：

1. **【真 bug，高危】projects.py 建项目 500**：2.6 清理导入时误删 `from pathlib import Path`，但 `create_project` 仍在用——所有新建项目都会失败。已恢复导入并端到端复验（无效包 400 → 建项目绑测试包 → ch7 404 → ch6 预览写入修订标题 → 下载 → 清理，全过）。
2. **pack_path 穿越防护缺失**：`pack_service.pack_path()` 声称防穿越但实际无校验；已补 `is_relative_to` 硬校验（`../../.env` 等攻击向量实测被拦截）。`get_pack` 的包 ID 穿越与 `safe_project_id` 的 9 组边界用例复测全拦。
3. **测试包不一致**：ch5 的 `next` 仍指向旧标题（渲染边界会失效）、reading/ch6.md 仍引用已删除的第七章；已同步为"（测试修订）"标题与模板尾部锚点。

**复测通过项：** 两包结构一致性（manifest/chapters/reading/template/next 链）、正式包项目特定词零残留、项目隔离回归（项目 1: 22/53/15 vs 项目 999 全空）、后端零残留引用且编译通过、前端零残留、四页面浏览器回归、增强功能 6 tab 正常、控制台零错误、进度条 29%（2/7 章）。

---

# 阶段三：多用户化 + 上线

> 目标：公网可安全访问，多人同时使用互不干扰。

## 步骤 3.1 前端 API 地址相对化 ✅

- `frontend/js/api.js` 第 5 行：`API_BASE = 'http://127.0.0.1:8000/api'` → `'/api'`。
- 全局检索 `127.0.0.1` 清零（含 index.html 设置页默认值）。
**验收：** 前端由后端同端口托管，功能正常。

**实施记录（2026-08-05）：**
1. `api.js` API_BASE 改 `'/api'`；`index.html` 设置页"后端API地址"输入框改 readonly `/api`（原本无 id 无 JS 引用的摆设项，加注"同源部署无需配置"）。
2. 检索确认前端硬编码清零；vendor/drawio xml 里的 `host="localhost"` 是 drawio 模板元数据，无关。
3. 后端 `config.py`/`run_server.py` 的 `127.0.0.1` 属步骤 3.6 范围，本步不动。
**验收（全过）：** 同源托管下网络面板确认全部请求走 `localhost:8000/api` 相对路径；四页面回归正常（步骤条 8 项/进度 29%/文档 2 份/设置页显示 /api）；控制台零错误。


## 步骤 3.2 加登录认证 ✅

**方案（轻量）：** 账号密码 + JWT（PyJWT / itsdangerous）。
1. DB 新增 `users` 表（id、username、password_hash、role、created_at）；首次启动生成管理员账号（密码从环境变量读，强制首登修改）。
2. 新路由 `POST /api/auth/login` 签发 token；前端加登录页，token 存 sessionStorage，所有请求带 `Authorization: Bearer`。
3. 后端写 `get_current_user` 依赖，逐个路由挂载（**folders、skills、enhancements 全部挂上，无一遗漏**）。
**验收：** 未登录访问任何 API 返回 401；登录后正常。

**实施记录（2026-08-05）：**
1. **后端**：`users` 表（含 must_change_password 列）；新增 `services/auth.py`（PBKDF2-SHA256 密码哈希、JWT 签发/校验、失败 5 次锁 15 分钟）；新增 `routers/auth.py`（/login、/me、/change-password）；鉴权不用逐个挂依赖而是用 **main.py 全局中间件统一拦截**（比逐路由挂载更不可能遗漏），公开路径仅 /api/auth/login 与 /api/health，/docs 接口文档也保护；首次启动从 ADMIN_INIT_PASSWORD 建 admin（未配则随机生成打印到控制台），JWT_SECRET 未配则进程内随机密钥并警告。
2. **前端**：全屏登录层 + 首次登录强制改密弹窗（sticky，ESC/点外不可关）；api.js 所有请求自动带 Bearer token，非登录接口的 401 自动清 token 弹回登录层；`downloadChapterDocx` 改 fetch blob（a 标签直链无法带 token）；aiCompose/importExcel 两处直连 fetch 也补了 token。
3. **验收中修复 2 个 bug**：① 登录接口 401 被误当"登录态失效"（前端对 /auth/login 豁免弹回逻辑）；② 失败锁定计数每次被清零（未锁时 rec[1]=0<now 恒成立，改为仅锁定期过后才重置）。
4. 依赖：requirements.txt 加 PyJWT==2.13.0；.env.example 补 JWT_SECRET/ADMIN_INIT_PASSWORD 说明（.env 不入库）。
**验收（全过）：** 未登录全部 API 401、/docs 401、/health 200；错密码提示正确且第 5 次锁定 15 分钟（423）；首登强制改密→改后自动进系统；新密码重新登录全流程（四页面/下载）正常；控制台零错误。
**注意：** 初始密码已在验收时消费，当前 admin 密码以验收改密后的新密码为准（已交付用户）。


## 步骤 3.3 消灭任意路径浏览 ✅

- 删除 `GET /folders/browse-any`（`routers/folders.py` 第 129 行）及前端"选择文件"路径选择器。
- 保留的 `/folders/browse`（限 DATA_SOURCE_BASE 内）也要挂认证。
**验收：** 直接请求 `/api/folders/browse-any` 返回 404。

**实施记录（2026-08-05）：**
1. **后端**：删除 `browse_any_path` 路由及其专用辅助函数 `_list_dir_items`（唯一调用方已删）、`platform`/`string`/`List`/`detect_file_type` 无用导入；`main.py` 的 SPA 兜底路由补上 `/api/*` 未知路径直接返 404 JSON（不再被兜底成 index.html，已删接口不会残留为 200）。
2. **认证**：`/folders/browse` 与 `/folders/scan` 由 3.2 全局中间件统一拦截，无需逐路由挂载（实测未带 token 返回 401）。
3. **前端**：设置页删除"默认输出路径/模板文件路径/申报材料路径"三个路径框及"选择文件"按钮（本机路径在 web 部署下无意义，模板已回退到材料包内置 template.docx）；app.js 删除 openLocalPicker/selectLocalFile/localStorage 路径持久化逻辑（SETTINGS_PATH_INPUT_IDS 等），新建项目选择器改为 `openSourcePicker` → `/folders/browse`，只能浏览 DATA_SOURCE_BASE 内目录；文件条目只展示不可选（只需选文件夹）。
4. **遗留**：章节预览/生成仍从 localStorage 读 templatePath/materialsPath（值永远为空，走包内模板兜底），将在 3.4 上传模式中随参数链路一并清理。
**验收（全过）：** 带 token 请求 `/api/folders/browse-any` 返 404、不带 token 返 401；`/folders/browse` 根目录 200、越界 `/etc` 返 403；任意未知 `/api/*` 路径返 404 JSON；浏览器端：设置页三路径框已消失、新建项目选择器锁定数据源根目录、子目录导航/选择回填正常、控制台零错误。

## 步骤 3.4 本机路径模式 → 上传模式 ✅

1. 新路由：`POST /projects/{id}/materials`（多文件/zip 上传，解压到 `workspace/projects/<id>/materials/`）、`GET /projects/{id}/materials`（列表）。
2. `skill_runner.run_chapter` 的 `materials_path` 改为按 project_id 解析到该项目材料目录；`materials_client` 的 root 同源改造。
3. 模板文件不再需要用户选路径——直接用项目绑定 pack 里的 `template.docx`（步骤 2.5 已铺好）。
4. 前端系统设置页：删除三个路径框与 localStorage 逻辑（`app.js` 第 22~38 行），改为材料上传入口。
**验收：** 全新用户在页面上传材料 zip 后即可生成，全程无"填路径"操作。

**实施记录（2026-08-05）：**
1. **后端接口**（`routers/projects.py`）：`POST /projects/{id}/materials`（多文件 multipart，zip 自动解压，多级文件夹结构原样保留）、`GET`（递归列表）、`DELETE`（清空）。zip 安全：逐成员路径穿越校验（恶意 zip 实测 400 且不落盘）、解压后总量 2GB/3000 文件上限防 zip 炸弹、Windows GBK 中文文件名乱码修正；上传前校验项目存在；Python 3.9 下 UploadFile 的 SpooledTemporaryFile 无 seekable，zip 先落盘临时文件再解压（用完即删）。
2. **生成链路**（`routers/skills.py`）：`chapter_run` 的 materials_path 留空时自动解析到 `workspace/projects/<id>/materials/`（非空才把 list_materials/read_document 工具挂给 Kimi）；模板骨架由 `_resolve_template_path` 自动回退包内 template.docx（3.3 已验证）。
3. **前端**：设置页新增"申报材料（当前项目）"区——上传/清空按钮 + 递归文件列表（按当前项目隔离，切项目/进设置页自动刷新）；api.js 新增 uploadMaterials/listMaterials/clearMaterials（FormData 上传时去掉 JSON Content-Type 交浏览器带 boundary）；删除 runChapter/getChapterContent/getChapterPreview 的 templatePath/materialsPath 参数及 app.js 三处 localStorage 路径读取（3.3 遗留一并清理）。
4. **附带**：删除项目时同步清理 `workspace/projects/<id>/` 数据目录。
**验收（全过）：** 多级文件夹+中文文件名 zip 上传解压正确（递归列表 5 文件）；恶意 zip（../../ 成员）400 拦截且不落盘；单文件直传/不支持格式跳过/清空正常；浏览器端设置页实际上传一个 PDF 成功（5→6 文件）、控制台零错误；**不传任何路径参数启动第 6 章生成成功**，生成内容含"尚未在现有材料中明确"的如实标注，证明材料读取工具已自动挂载（测试用假 PDF 读不出正文属预期）。注：验收时 demo 项目第 6 章被重新生成了一次（内容正常）。

## 步骤 3.5 数据按用户隔离

1. `projects` 表加 `user_id` 列；所有项目查询带 `WHERE user_id = 当前用户`。
2. `workspace/projects/<id>/` 目录归属校验：后端按 DB 中 project→user 关系鉴权，防止跨用户访问文件。
3. 生成任务状态从内存字典迁入 DB 新表 `generation_jobs`（project_id、chapter_n、status、data_json、error、updated_at）——多 worker 部署的前提。
**验收：** 两个账号同时操作，列表、章节、生成状态互相不可见。

## 步骤 3.6 部署配置环境化

- `config.py`：`APP_HOST`/`APP_PORT` 改从环境变量读（默认 127.0.0.1:8000 保持本地体验）。
- CORS：`allow_origins` 从环境变量读正式域名，去掉 `*`。
- 密钥：Kimi/天眼查 key 只从环境变量读；`.env` 仅供本地开发，不进镜像/仓库。
-  Kimi 调用加每用户速率限制（如 slowapi），防止 key 被刷。

## 步骤 3.7 容器化与发布

1. 编写 `Dockerfile`（python:3.12-slim 基础镜像，装 PyMuPDF/lxml 依赖，多 worker uvicorn 或 gunicorn）+ `docker-compose.yml`（app + 数据卷）。
2. Nginx 反代：HTTPS 证书、静态文件直出、`/api` 转发、上传大小限制（材料 zip 可能上百 MB，调大 `client_max_body_size`）。
3. 数据备份：`workspace/` 挂卷，每日快照。
**验收：** 干净服务器上 `docker compose up` 一条命令起全站；HTTPS 访问正常；重启容器数据不丢。

## 阶段三总验收（上线 checklist）

- [ ] 未登录全部 API 401；`browse-any` 404
- [ ] 两账号并发使用互不串数据、互不覆盖生成结果
- [ ] 抓包确认 CORS 仅放行正式域名、全站 HTTPS
- [ ] 代码库、镜像、日志中检索不到任何真实密钥
- [ ] 压测：单章生成接口并发 5 个不报错（任务排队/状态正确）

---

# 附：风险与注意事项

1. **顺序不可乱**：阶段二改存储路径（2.4）会影响阶段三上传模式（3.4）的落点，先做完 2.4 再做 3.4 可少返工。
2. **web_render.py 热重载机制**（`routers/skills.py` 的 `_load_web_render`）在迁移进模板包后必须保留，否则"改写作要求即时生效"的优势丢失。
3. **删除旧管线（2.6）前**先确认无线上用户依赖 `/projects/{id}/generate` 整本生成入口；如有，先在新管线补"一键全七章生成"再删。
4. **每个阶段结束打 git tag**（v1.1-bugfix / v1.2-packs / v2.0-online），出问题可回滚。
5. 报告与本文档（`问题核实报告.md`、`修改步骤.md`）建议随源码入库，作为重构的依据存档。
