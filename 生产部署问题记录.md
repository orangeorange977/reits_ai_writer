# 生产部署问题记录（2026-08-07）

## 一、背景

本地已完成「申报材料页材料可见性改进」与「AI 生成内容溯源」两项功能（详见下文改动清单），
需部署到生产服务器：

| 项 | 值 |
| --- | --- |
| 主机 | 193.112.194.61（腾讯云轻量，广州，Ubuntu 24.04） |
| SSH | 端口 22，用户 ubuntu（sudo 免密、已加入 docker 组） |
| 项目目录 | `~/reit-app` |
| 部署方式 | `docker compose up -d --build`（数据卷不受影响） |

## 二、当前问题：SSH 连接被服务器封禁，无法部署

### 现象

- 22 端口 TCP 三次握手**成功**，但连接建立后**立即被重置（RST）**，连 SSH 版本横幅都收不到；
- 本机多次重试（paramiko、裸 socket）均被秒拒，错误统一为 `Connection reset by peer`；
- 80 端口网站与 API **完全正常**（HTTP 200，`/api/auth/login` 正常响应）——服务器本身运行正常。

### 诊断结论

服务器端的 **fail2ban（或等效的防暴力破解机制）封禁了本机当前的公网出口 IP**：

- 本机当前公网出口 IP：**`111.55.210.70`**
- 「TCP 放行 + 立即 RST + 稳定复现」是 fail2ban 使用 reject 规则的典型行为；
- 封禁可能由此前的失败登录尝试或历史连接触发。

由于 SSH 完全进不去，**无法从本机侧自行解封**，必须经腾讯云控制台处理。

## 三、解决方案（三选一）

### 方案 A（推荐）：解封 IP

通过**腾讯云控制台 → 远程登录 / Orca 终端**（网页版命令行，不依赖 SSH）进入服务器：

```bash
# 1. 查看 sshd  jails 中被封禁的 IP，确认含 111.55.210.70
sudo fail2ban-client status sshd

# 2. 解封
sudo fail2ban-client set sshd unbanip 111.55.210.70
```

若服务器未安装 fail2ban，改为排查：

```bash
sudo iptables -S | grep 111.55.210.70   # iptables 拦截规则
cat /etc/hosts.deny                      # TCP wrappers
```

### 方案 B（省事）：临时停掉防暴力破解服务

```bash
sudo systemctl stop fail2ban
```

部署完成后再 `sudo systemctl start fail2ban` 恢复。

### 方案 C：用户自行部署

由本机把代码打包交给用户，在控制台终端执行：

```bash
cd ~/reit-app
# 覆盖代码后：
sudo docker compose up -d --build
```

## 四、本次待部署的改动清单（已在本地完成并验证）

### 问题一：上传材料在"申报材料"页可见（体验改进）

| 文件 | 改动 |
| --- | --- |
| `backend/routers/projects.py` | 新增 `_rel_parts()` 路径净化；非 zip 上传按相对路径落盘，保留文件夹目录结构（`..` 直接丢弃防穿越） |
| `frontend/js/api.js` | 上传时携带 `webkitRelativePath` 作为 multipart 文件名 |
| `frontend/index.html` | page-ndrc 新增「📁 申报材料」面板（默认展开，上传文件/上传文件夹/清空） |
| `frontend/js/app.js` | `toggleMaterialsPanel()`（自包含切换，不干扰增强面板）、`_renderMaterialsTree()` 树形列表、`loadMaterialsUI()` 双端渲染、进 ndrc 页自动刷新 |

### 问题二：AI 生成内容溯源（业务化，不展示技术细节）

> 设计约束：系统面向金融业务人员，**不展示**工具名、参数、JSON 等任何技术细节。

| 文件 | 改动 |
| --- | --- |
| `backend/services/skill_runner.py` | ① 输出契约新增规则 8：每个块必须带 `src` 来源标注（申报材料：<路径>/摘要表：<字段>/天眼查查询：<企业名>/网络公开信息/planning.md/固定表述/待核实）；② `_block_to_html` 渲染「📎 依据」行、`_HTMLToBlocks` 解析 `doc-src` div 回挂 `src`（编辑区可改写、保存往返不丢）；③ `run_chapter` 记录业务化参考材料清单 `refs`（planning.md/本章写作要求/已保存摘要/实际读取的材料文件路径/天眼查查询企业名），随 `ch{n}.json` 落盘；④ `get_chapter_content` 返回 `refs`、`save_chapter_content` 保留 `refs` |
| `frontend/js/app.js` | 章节编辑区新增「📋 本章生成参考了以下材料」清单面板 |
| `frontend/css/style.css` | `.doc-src`、`.refs-panel`、`.ref-item` 等样式 |

### 本地验证结果

- ✅ `py_compile` 后端文件通过
- ✅ 路径净化单元测试（含 Windows 盘符、`..` 防穿越）
- ✅ `src` 标注 HTML ↔ 结构化块往返测试（含人工改写溯源行）
- ✅ 输出契约规则 8 存在性检查
- ✅ Word 渲染脚本 `web_render.py` 只读固定字段（type/text/caption/rows/headers），新增 `src`/`refs` 字段自动忽略，不会混入导出文档

### 注意事项

- **旧章节没有溯源数据，需重新生成**；
- 溯源标注仅在编辑区可见，**不进最终 Word**。

## 五、解封后的部署步骤（执行方：AI 助手，paramiko）

1. 测试 SSH 连通（`sshx.py run 'hostname'`）；
2. 勘察服务器 `~/reit-app` 现状（docker-compose.yml、.env、容器状态）；
3. 本地打包代码（**排除** `workspace/`、`.env`、`*.log`、`__pycache__` 等运行期数据）并上传；
4. 服务器解压覆盖后执行 `sudo docker compose up -d --build`；
5. 在线验证：登录 → 申报材料页材料面板 → 生成章节 → 查看「📎 依据」与参考材料清单。

## 六、状态

- [x] 本地功能开发与验证
- [ ] 服务器 fail2ban 解封（等待用户通过腾讯云控制台操作）
- [ ] 代码上传与重建部署
- [ ] 生产环境在线验证
